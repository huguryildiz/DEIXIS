{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://deixis.local/contracts/research/research-title.schema.json",
  "title": "ResearchTitle",
  "description": "A short title derived from the research question and the included sources, written before a grounded answer exists. It is replaced by the grounded answer title once a structurally valid answer is saved.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "step_input_id",
    "scope_revision",
    "skill_package_hash",
    "title"
  ],
  "properties": {
    "schema_version": { "type": "string", "const": "deixis.research_title.v1" },
    "step_input_id": { "$ref": "common.schema.json#/$defs/step_input_id" },
    "scope_revision": { "$ref": "common.schema.json#/$defs/scope_revision" },
    "skill_package_hash": { "$ref": "common.schema.json#/$defs/skill_package_hash" },
    "title": {
      "type": "string",
      "minLength": 1,
      "maxLength": 200,
      "description": "Concise descriptive title, at most 15 words, rewritten from the question and sources."
    }
  }
}
