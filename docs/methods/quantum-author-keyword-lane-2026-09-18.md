# Isolated author-keyword inheritance lane

**Status:** separate post-hoc development diagnostic. It does not modify the DEIXIS product search default, the completed B/C comparison, the survey run, or the earlier exact-abstract-phrase cited-term run. The quantum question and several misses are already known, so any overlap is descriptive.

From the completed C candidate pool, select the three highest OpenAlex citation-count records that pass the cited-term lane's title/abstract and non-survey gates **and have an IEEE DOI**. This DOI restriction is a metadata-source constraint, not a claim that IEEE papers are better. Freeze their identity, ranking, source hashes and citation-count snapshot before IEEE lookup. Citation count only prioritizes records.

Make at most three DOI-specific IEEE Xplore Metadata API requests, one per frozen DOI, with the configured key. Use the returned `author_terms` field only, preserving a distinction from IEEE controlled terms, OpenAlex-generated terms, model-proposed text, and paper full text. A missing or unauthorized response remains visible; do not silently substitute another source. Exclude unsafe, generic, and previously searched exact phrases. Rank remaining terms deterministically by number of distinct source papers, then multiword specificity, then seed citation rank and lexical order. Select at most four, representing at least two source papers, and compile one `entanglement AND (term1 OR …)` OpenAlex title-and-abstract query. Freeze the metadata projection, rejected and selected terms, query, hashes, access outcomes, and two-request budget before any OpenAlex search.

Run **one round**, at most two OpenAlex pages of 100 results: two logical requests, 200 returned slots maximum. Save raw OpenAlex responses under ignored `.local/`, count returned, exact DOI/OpenAlex identities, and incremental identities relative to C. No recursive keyword learning, PDF extraction, citation traversal, or human screening is performed. A provider failure is not a zero-result success. Do not use the real library or port `8765`; never store or display API keys.
