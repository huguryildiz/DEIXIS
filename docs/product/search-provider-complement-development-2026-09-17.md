# Provider complement: development probe

**Status:** frozen before these Semantic Scholar searches. The six LitSearch cases have already been evaluated and are development data. This probe cannot serve as independent validation.

Use each question's frozen compact-vocabulary model output from `.local/search-compact-development-2026-09-17/`. Keep its OpenAlex core-only top-100 response unchanged. Compile one plain Semantic Scholar query from the first compact core term and first facet term with the existing provider compiler, and request up to 100 relevance-ranked records from Semantic Scholar. No LLM call, term editing, or fallback is allowed in this probe. Record the exact query, response status, retries, payload hash, access mode, and returned count. The S2 request has a larger requested depth than the former OpenAlex second request (100 versus 25), so this is a provider-complement diagnostic, not an equal-cost A/B comparison.

Measure distinct frozen LitSearch gold DOIs in OpenAlex alone, Semantic Scholar alone, and their union for each question. Check each gold DOI's existence in the searched indexes separately from its rank in the response. Do not infer precision from additional records. A multi-provider candidate is promising if the union finds at least one gold work in four of six questions and Semantic Scholar adds gold works in at least two questions. It still requires blinded relevance assessment and a fresh case set before changing DEIXIS. Save outputs in ignored `.local/search-provider-complement-2026-09-17/`; leave the live library and port 8765 untouched.
