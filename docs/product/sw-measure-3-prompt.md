# Task: the third D88 measurement (13ö): one topic × quick / standard / detailed, after 13f + 13g + 13h

Repo: `/Users/huguryildiz/Documents/GitHub/DEIXIS`. A measurement only: no product code changes. Results go to
`.local/sw-measure-2026-09-24/`. Nothing is written to `docs/` until the owner has read the result and said "uygun";
then row 13ö in `docs/product/sw-status.md` and the D88 table get the new numbers. Implementer: Opus · high. Written
2026-09-23 at the end of the slice 13h conversation; read this whole file before anything else.

## Where things stand (2026-09-23)

- The second 13ö run (`.local/sw-measure-2026-09-22b/`, commit `8fa06b4`, after 13e) took `quick` 9.1 min, `standard`
  26.2 min and `detailed` 45.7 min, from creating the research to the finished answer. Discovery was the largest item.
  The owner then set the targets to at most **10 / 15 / 20 min** (D88). The targets are goals, not limits.
- Three slices landed since then and none has been measured end to end:
  - **13f** (D89): discovery searches run in parallel across hosts. It had no wall-clock measurement.
  - **13g** (D90, D91): the query repair, and `detailed` reads 1,000 records per query.
  - **13h** (D92, commits `0f441ca` and `13e085d`): a model writes the sw query. The product default is
    `search_query = "model"`, and the code's query is searched beside it. This adds one model call before the approval
    card and more first-round queries.
- Quality baseline for the quantum question, from the 13h live acceptance
  (`.local/sw-slice13h-acceptance-2026-09-23/result.md`): **23 of the 31 verified works** were in the first 1,000
  records of the queries on every call. The 31 are `positives("q1")` in
  `.local/sw-vocabulary-experiment-2026-09-23/common.py`: confirmed mathematical models with high confidence from
  `.local/quantum-work-adjudication-2026-09-18/adjudication.jsonl`. That acceptance measured candidate records only.
  This run measures what the whole product keeps, reads and cites.

## What to run

1. Make a new folder `.local/sw-measure-2026-09-24/`. Copy `campaign.py` and `numbers.py` from
   `.local/sw-measure-2026-09-22b/` without changes. Add `DEIXIS_SEARCH_QUERY=model` to the server environment. It is
   the default, but the run must not depend on that. Record this as the only change to the script.
2. Before the first call, write `protocol.md` (frozen expectations, the same shape as `../sw-measure-2026-09-22b/protocol.md`):
   - run identity: the commit, a clean tree apart from the owner's `TODO.md`, `.vscode/` and `scripts/local_index.py`,
     and the model;
   - what is measured;
   - the expectations, with numbers;
   - the known limits.

   Do not edit this file after the runs; every deviation goes into `result.md`.
3. Run: `MEASURE_TAG=-luna MEASURE_CONNECTION=codex MEASURE_MODEL=gpt-5.6-luna MEASURE_MODEL_EFFORT=medium
   MEASURE_CODEX_HOME=<the signed-in codex-home the earlier campaigns used> uv run --no-sync python campaign.py quick
   standard detailed`, in the background, with one log per effort.
   - Keep everything else as in the second campaign: the same question, `source_scope: academic`, embeddings `off`,
     the protocol approved as proposed, and the efforts run one after another. **Constants are not tuned.**
   - Ports 8801, 8802 and 8803, each with its own data directory.

## What to measure

- **Time:** wall clock per effort and per stage (the same marks as the second campaign), side by side with 9.1 / 26.2 /
  45.7 and with the targets 10 / 15 / 20. Show separately:
  - the new `search_query` step: its wall time and whether a repair was needed;
  - discovery with 13f's parallel search: parallelism as the sum of step durations divided by stage wall time, as
    before;
  - `detailed`'s reads at 1,000 per query.
- **Quality**, against the 31 verified quantum works, per effort, matched by DOI or normalised title as `common.hits`
  does. Read each effort's own `library.sqlite` in its data directory; never read the product's. Report:
  1. **in the pool**: records the discovery runs stored;
  2. **kept**: records that passed screening (abstract stage);
  3. **read**: records whose full text was fetched and judged;
  4. **cited**: records cited in the answer.

  Also say which of the 31 came only from the model query and which only from the code query. Use `origin` on the
  stored queries and the search step each record came from.
- **The query itself:**
  - the model's terms and their kinds;
  - the count warnings;
  - both OpenAlex query texts;
  - the second round's additions;
  - the number of provider requests per round.
- **Cost:** model calls per task type, provider requests, and downloads.
- **Carried over from the last run:** whether arXiv returned any record, and whether the answer's repair round ran.

## Stop conditions

- Luna's quota runs out, or a run pauses with `model_call_failed`. Stop, record where it stopped, and report. **Do not
  switch to another model or connection silently.** A run on another model is a separate campaign and needs the
  owner's decision.
- A run pauses with `search_query_failed`. Record it as a finding, which is exactly what D92 is meant to show. Do not
  choose the code query on the run's behalf. Stop that effort and report.
- Never start, stop or query the service on 8765, and never open the default data directory's `library.sqlite`.

## Output

`result.md` in the new folder:

1. The answer in plain Turkish first: did each effort meet its target, and how many of the 31 were kept and cited.
2. Then the tables.
3. Then what was not measured: one topic and one run per effort, so no spread; the network and providers on that day;
   answer quality beyond the count of verified works cited.

Then stop and wait for the owner's "uygun" before touching `docs/`.
