# SW uygulaması: ana dosya

SW1–SW17'nin ürüne işlenmesi bu dosyadan yürür. Her sohbet (plan, uygulama, inceleme) önce burayı okur: sırada ne var, hangi dilim dosyası ve hangi prompt kullanılacak, hangi karar bekliyor. Kararların kendisi [search-workflow-review-2026-09-18.md](search-workflow-review-2026-09-18.md)'dedir; dilimlerin kapsamı, kabul koşulları ve tüm dilimler için geçerli kurallar [sw-implementation-plan.md](sw-implementation-plan.md)'dedir. Bu dosya yeni tasarım taşımaz; doğruluk kaynağı kod ve testlerdir.

## Bir tur

1. **Plan** (Fable, plan sohbeti): sıradaki dilimin dosyası ve promptu yazılır; satır `dosya hazır` olur.
2. **Uygulama** (yeni sohbet, Opus, efor medium): ilk ileti yalnızca `docs/product/sw-sliceNN-prompt.md dosyasını uygula.` olur. Satırını `uygulanıyor` yapar; iş bitip testler yeşil olunca satırı `uygulandı, inceleme bekliyor` yapar, açık kalanı yazar, tek commit olarak işler ve `main`'e iter. Yarım kalan iş commit'lenmez.
3. **İnceleme** (yeni sohbet, uygulayandan farklı model; hangi model aşağıdaki kurala göre). Her dilim incelenir, ama aynı ağırlıkta değil (sahibin kararı, 20 Eylül 2026):
   - **tam:** dilim biter bitmez, tek başına. Uygulayan sohbetin commit'i dilim dosyasına karşı okunur, testler koşulur, dosyada ve testlerde olmayan durumlar aranır (tekrarlanan çağrı, kalıcı silme, mevcut seçim ve sürüm mantığıyla etkileşim), bulgular ayrı bir düzeltme commit'iyle işlenir. Yanlış kararın sessizce kanıtı bozduğu dilimler böyledir: 02, 03, 05, 09, 12, 16 ve açılırsa 23. Dilim 01 temel olduğu için tam incelendi.
   - **toplu:** iki üç dilim birlikte, doğal bir durakta, tek oturumda; dilimlerin birbirine oturuşuna da bakılır. Üst üste kurulan omurga dilimleri (09 → 10 → 12) biriktirilmez: 10, 12 başlamadan önce incelenir.
   - **her dilimde, sahip:** uygulayan sohbetin son iletisindeki "yazıldığı gibi yapılamayan yerler" ve "yapmadıklarım" listesine ve test sayısına beş dakikalık bakış. Listede şüpheli bir şey varsa dilim toplu yerine tam incelenir.
   - Dilim ancak incelemesi bitince `kapandı` olur; toplu bekleyen dilim `uygulandı, inceleme bekliyor` kalır ama sonraki dilimin başlamasını engellemez. Her inceleme aşağıdaki **Yapılan incelemeler** kaydına bir satır yazar; satır silinmez.
4. **Ölçüm en sonda, toplu yapılır** (dilim 24: kuantum yeniden, yeni bir soru, Elicit raporlarıyla karşılaştırma). Dilimler ölçüm için durmaz; ölçülmemiş parça "ölçülmedi" diye kaydedilir. İstisnalar: dilim 06'nın bilinen kusuru ve dilim 13'ün duman testi.
5. **Dilim 13'ün dört koşusundan çıkan ara dilimler (22 Eylül 2026):** 13a → 13b → 13c → 13d, sonra ölçüm adımı (üç efor × bir konu, Sonnet'e verilir, süreler D88'in altına yazılır), sonra ana planın 14'ü. 13a tam incelenir (model sınırı); 13b–13d toplu. 13a'nın kabul koşulu DeepSeek ile beşinci duman koşusudur (`data-5`), o koşu dilim 13'ün planıyla yapılır.

Plan turu her dilimde Fable · high ile yapılır. **İncelemenin varsayılanı Fable değildir** (sahibin kararı, 21 Eylül 2026): toplu incelemeler ve her dilimin beş dakikalık sahip bakışı Codex'te `gpt-5.6-sol` · high ile koşar — uygulayandan farklı bir model olduğu için kural bozulmaz ve ayrı abonelikten koştuğu için haftalık Fable kotasına dokunmaz. Fable · high yalnızca yukarıda **tam** diye işaretli dilimlerde (02, 03, 05, 09, 12, 16 ve açılırsa 23) ve bir incelemenin muhakeme gerektiren bir şey bulup yükseltilmesi gerektiğinde kullanılır. İnceleme her hâlde önce diff'i okur, dilim dosyasını elde tutar ve gerektiği kadar dosyaya iner; bütün dokunulan dosyaları baştan okuyan bir oturum aynı bulguyu birkaç katı jetona çıkarır. Gerekçe: inceleme türetme değil doğrulama işidir, ve 21 Eylül 2026'da haftalık kota 15 saatte %27'ye inmişti. Tablodaki sütun yalnızca uygulayan sohbeti gösterir. **medium:** tasarımı dilim dosyasında bitmiş, mekanik işler. **high:** uygulayanın köşe durumlarında kendi yargısını kullanması gereken dilimler (karar kuralları, `selections` türetmesi, sürüm birleştirme, sözleşme + yöntem paketi + akışın birlikte değiştiği yerler). Duman testi ve ölçüm kampanyası Fable'da koşar, çünkü protokolü dondurmak ve sonucu yorumlamak muhakeme işidir; uzun koşan ölçüm betiği arka plan ajanına (Sonnet) verilebilir. Bir dilimin son iletisinde "yazıldığı gibi yapılamayan yerler" listesi uzunsa sonraki benzer dilimin eforu bir kademe yükseltilir.

Durumlar: `plan` (yalnızca ana planda) · `dosya hazır` · `uygulanıyor` · `uygulandı, inceleme bekliyor` · `kapandı` · `sahip kararı bekliyor`.

## Dilimler

| # | Dilim | Tür | Uygulayan model · efor | İnceleme | Durum | Dosya · prompt | Commit / D | Açık kalan |
|---|---|---|---|---|---|---|---|---|
| 01 | Protokol kaydı ve belirlenimcilik | Kur | Opus · medium | tam · yapıldı | kapandı | [dilim](sw-slice01-protocol-and-determinism.md) · [prompt](sw-slice01-prompt.md) | `5b5c5bb` + inceleme düzeltmesi · D70 | Protokol gövdesinin ölçüt/blok/sinyal alanları `None`; `attached` araştırmada protokol kaydı yok (sahip kararı); uygulama-geneli varsayılan gözden geçirici gövdeye çözülmüyor (`build_protocol` deposuz); `code_version` commit başına değişmiyor (dilim 24 öncesi); FTS eşitliği yalnızca ikinci anahtarla sabit, ölçülmedi; `search_workflow` yalnızca saklanıyor. Slice SQL'inden sapma: `protocol_records_no_delete` tetikleyicisine 0010'daki kalıcı-silme istisnası eklendi. |
| 02 | Aşama kararlarının saklanması | Kur | Opus · high | tam · yapıldı | kapandı | [dilim](sw-slice02-stage-decisions.md) · [prompt](sw-slice02-prompt.md) | `a3919af` + inceleme düzeltmesi · D71 | Hiçbir akış adımı bu tabloları yazmıyor (yazanlar dilim 05, 07, 09, 12, 16); neden kodları yalnızca SW11'in çalışma adları, derleme işareti / kod kapısı / K3 bütçe kodları yok; `criterion_not_met` `selections`'ta `excluded` görünüyor, ayrımı hiçbir görünüm göstermiyor; sürümler çeliştiğinde iş `pending` kalıyor ve hiçbir yerde gösterilmiyor; `apps/web/src/api.ts` `origin` tipi `code_rule`'u tanımıyor, ilk yazan dilimde genişletilmeli; SW9.5 konu özellikleri ve geniş dışa aktarım yapılmadı. Slice'tan sapma: `purge_sources` (D65), kaynak düzeyinde yetki tablosu olmadığı için karar silme tetikleyicisini araştırma purge yetkisiyle açıyor. İncelemede düzeltildi: `undo_human` geri getirdiği karara güncel revizyonu damgalıyordu (eskimiş karar taze görünüyordu); `work_outcome` aynı sonuçlu sürümlerden ilk yazılanı adlandırıyordu, şimdi işin başı, yoksa en küçük kimlik. Yoruma açık nokta: insan kararı en yenisi alınırken eşit zaman damgasında yazma sırası belirliyor. |
| 03 | Kayıt türü ve sürüm birleştirme | Kur | Opus · high | tam | kapandı | [dilim](sw-slice03-record-kind-and-version-links.md) · [prompt](sw-slice03-prompt.md) | `67f5853` + inceleme düzeltmesi · D72 | 127 çift incelemede okundu: 4 birleşme doğru görünüyor; 23 `extended_version` çiftinin çoğu bildiri + dergi değil, iki DOI altında kayıtlı tek makale (iki iş kalıyor, iki kez taranacak; dilim 05 / 20). `pairs.csv` `.local/sw-slice03-pairs-2026-09-20/`'de, çiftlerin 85'i Zenodo/figshare yatırımı olduğu için `artifact` + `artifact` çıkıp hiç bağlantı almıyor, 4 çift birleşiyor. Dış bağlantılar (SW6.4) dilim 05'te; ek ürün ve bildirimler aday listesinde kalıyor (dilim 09); `probable_version` insan kuyruğuna dilim 16'da giriyor; `extended_version` çiftinin tek sayılması hiçbir yerde yok (dilim 20); bağlantılar, geri alma ve "may duplicate" işareti arayüzde yok; işin başı hâlâ D48'in dar ön baskı testiyle seçiliyor; iki yayımlanmış kayıt aynı ön baskıyı isterse ilk işlenen alır (sıraya bağlı, belirlenimcilik kümesinde yok); eşikler tek konuda elle seçildi, DOI'siz kayıtlar ve ürün içi ölçüm yok (dilim 24). Slice'tan sapmalar: `undo_json` slice'ın adlandırmadığı `keep_work_id`'yi de taşıyor (yoksa hangi işe taşındığı bulunamıyor); `notice_type` normalleştirilmiş değil ham başlığı okuyor (iki nokta normalleştirmede kayboluyor); `_keep_and_drop` "published kaydın işi"ni iş düzeyinde okuyor (yayımlanmış kaydı barındıran iş); sınıflandırılacak her çiftin özeti okunuyor, yalnızca başlık eşiğini geçenlerin değil. |
| 04a | Kodla sözcük dağarcığı ve kavram blokları (çıkarma, sayım sınaması, blok sorgusu, `sw` keşfine bağlama) | Kur (ölçülmedi) | Opus · high | toplu | kapandı | [dilim](sw-slice04a-code-vocabulary.md) · [prompt](sw-slice04a-prompt.md) | `dabfa37` + inceleme düzeltmesi · D73 | Plandaki dilim 04 ikiye bölündü. Sayfalama ve `sw` okuma bütçesi dilim 04c'ye yazıldı. Kuru çalıştırma: kuantum sorusunun 2. ve 3. cümlesindeki 8 ifade (`treatment`, `supporting`, `state`, `verified`, `full text`, `model feature`) bloklara giriyor, paket sorusunda `by simulation` iddia sayılıyor — atama kuralı soruyu tek parça okuyor, düzeltme `key_terms` ya da dilim 08. Sapmalar: sınama sırası `setting` sonra `task` (Extraction soru içi sırayı taşımıyor); düşen ifadenin sözcükleri sınanmıyor; soru kalıbı her cümlenin başında atılıyor; `key_terms` verilmeyen revizyon öncekini taşıyor (temizleme yok); `code_version` blok derleyicisini adlandırıyor. Yapılmadı: sayım istekleri `provider_requests` sayacına girmiyor; `views.research_view` `sw` koşusunda `plan` alanını `None` bırakıyor (arayüzde sözcük dağarcığı görünmüyor, dilim 08); duraklatma nedenleri arayüzde ham metin. |
| 04b | Veriden genişleme (yazar anahtar sözcükleri, başlık n-gramları; SW2.4) ve terim başına verim | Kur (ölçülmedi) | Opus · high | tek başına · yapıldı | kapandı | [dilim](sw-slice04b-data-expansion.md) · [prompt](sw-slice04b-prompt.md) | `d6a1753` + inceleme düzeltmesi `9faac65` · D76 | Önkoşul 04a, 04c, 04d (kapandı). Migration `0042`, karar D76. Yeni: `domain/expansion.py` (aday ifadeler), `workflow/expansion.py` (alan sınaması, ikinci tur dağarcığı, `term_yields`), `tests/test_expansion.py`, `tests/test_expansion_flow.py`; `flow._expansion` + `_freeze_expansion` ilk tur ile tarama arasında koşuyor. **Canlı kuru çalıştırma** (`.local/sw-expansion-dry-run-2026-09-21/`, 40 sayım isteği): kuantum sorusunun 1.369 kaydından 20 aday çıktı, hepsi alanın gerçek terimi; 12'si kabul, 8'i reddedildi ve her ret `below_field_share` (reinforcement learning 242.184/154, resource allocation 213.686/203, communication networks 105.049/487, quantum computing 66.151/1.406). Eşikler sonradan değiştirilmedi. **Görülen zayıflık:** kabul edilenlerin çoğu görev bloğunun değil ortam bloğunun başka adları ("quantum internet", "quantum repeater"); bunları yalnızca kapsanmışlık kuralı eler ve o kural ancak ortam terimleri köke inmişse (yani sayımlar okunabilmişse) iş görür. Kuru çalıştırmada sözcük dağarcığı adımı koşmadığı için terimler öbek hâlindeydi; kök biçimleriyle aynı havuz "repeater networks", "repeater chains", "routing protocols" gibi gerçekten genişleten ifadeler veriyor (aynı dosyada `coverage_variants`). N-gram kuralı öbeğin içindeki işlev sözcüğüne izin veriyor, bu yüzden "routing for quantum" gibi bir ifade de sınanabiliyor. Sapmalar: PubMed'de yalnızca `Owner="NOTNLM"` anahtar sözcük listeleri alınıyor (NLM DTD'si, 21 Eylül 2026: `Owner` varsayılanı `NLM`, yani sahipsiz liste dizinleyicinin); IEEE yolu bir canlı aramayla doğrulandı (21 Eylül 2026, üç kayıt): `index_terms` `ieee_terms` / `author_terms` / `dynamic_index_terms` diye üçe ayrılıyor, yalnızca `author_terms` alınıyor (`dynamic_index_terms` IEEE'nin makine üretimi terimleri). `candidates` kayıtlarda `work_id` okuyor (dilim dosyası yalnızca iş başlarını veriyor; kural aynı, ama iki sürümün tek sayılması böylece birim testinde sınanabiliyor); `term_yields` protokolden okuyor ve satırlara dilim dosyasının adlandırmadığı `form` alanını (terimin sorgudaki biçimi) ekliyor; `yield_at_expansion` protokol dondurulmadan önce hesaplanıyor (`count_yields` terimleri doğrudan alıyor), yoksa sayımlar saklanmadan önce protokol yazılmak zorunda kalırdı; yazar sözcükleri `ProviderRecord`'a girmeden önce sağlayıcıda temizleniyor (`common.keywords`) ve sütuna `_store_author_keywords` yazıyor, o da 0042 öncesi şemayla koşan tarih testi için sütun denetimi taşıyor. Mevcut testlerin hiçbirinin beklentisi değişmedi; `tests/test_search_paging.py`'deki sahte sayım yanıtı 40'tan 8'e indirildi, çünkü o dosyanın SYNTHETIC başlıkları alan sınamasını geçip her testte ikinci bir tur açıyordu — o dosya sayfalamayı ölçmeye devam ediyor, ikinci turun kendi testleri var. Yapılmadı: kabul edilen terimler kullanıcıya gösterilmeden sorguya giriyor (dilim 08, SW2.6); verim hiçbir ekranda yok (dilim 08/20) ve `run_steps` görünümünde taşınmıyor; özet n-gramları ve koşullu model adımı (SW2.5) yok; üçüncü tur yok; OpenAlex'in atanmış `keywords`'ü ve Scopus `COMPLETE` alınmıyor; tarama hâlâ `max_candidates` ile kesiliyor (dilim 09, K3), yani ikinci turun getirdiği kayıtlar çoğunlukla kuyrukta bekliyor; sayım istekleri hâlâ `provider_requests` sayacına girmiyor (04a ile aynı). İncelemede düzeltildi: aday ifadelerin havuzu, yalnızca bir genişleme kolunun bulduğu kayıtları dışarıda bırakıyor (aynı kapsamda ikinci keşif koşusu, bir öncekinin eklediklerinden yeniden öğrenip sorudan uzaklaşıyordu); verim sayımı 3.000 adayda 1 saniyeyi aşan pencere karşılaştırması yerine tek alt dizi aramasıyla eşleşiyor. Görülüp bırakılan: n-gram kuralı yalnızca uçlara bakıyor, kök biçimli havuzda "networks using", "repeater based" gibi adaylar çıkıyor (alan payı sınamasının elemesi beklenir, ölçülmedi); kabul edilen ifadeler görev bloğunun eş anlamlısı değil alanın yan konuları ("repeater networks", "routing protocols"), yani ikinci tur aramayı konunun komşularına genişletiyor — değeri ancak dilim 24'te ölçülür. |
| 04c | `sw` aramasında sayfalama ve okuma bütçesi | Kur | Opus · medium | tek başına · yapıldı | kapandı | [dilim](sw-slice04c-paging-and-read-budget.md) · [prompt](sw-slice04c-prompt.md) | `fb486b8` + inceleme düzeltmesi `78ae7e5` · D75 | Önkoşul 04a; dilim 07'nin önkoşulu. Migration `0041`, karar D75. Dokuz sağlayıcıya `cursor` eklendi (OpenAlex/bioRxiv imleç, yedisi ofset, SerpApi tek sayfa), `Connector` üç alan kazandı (`paging`, `max_reachable`, `page_gap`), `flow._search` sayfa alabiliyor ve yeni `_search_pages` bir sorguyu sayfa sayfa okuyor. Okuma sınırı `SW_READ_LIMIT = 2.000`, elle seçildi, ölçülmedi (dilim 24). Task 5 ölçümü (`.local/sw-paging-timing-2026-09-21/timing.json`, 1.368 gerçek + 132 SYNTHETIC başlık, 200'lük sayfalar, 1.500 aday): en yavaş sayfanın `record_search`'ü 0,75 sn, bunun `link_records` payı 0,25 sn, sekiz sayfanın toplamı 4,03 sn — sayfa başına 1 saniyenin altında, `links.py` değiştirilmedi. Sapmalar: `extra_requests` dilim dosyasının dediği gibi `_search_pages` içinde değil, `_discovery`'de döngüden hemen önce bir kez hesaplanıyor (modül düzeyinde `extra_page_requests`), çünkü hesap koşunun bütün sorgularını görmek zorunda ve `_search_pages` yalnızca tek sorgu alıyor; akış testleri sayfa boyutunu 200 yerine 20'ye, Semantic Scholar'ın 1.000'lik derinliğini 50'ye indiriyor (fikstürleri küçük tutmak için; gerçek sayılar `tests/test_providers.py`'de sınanıyor). Belgeyle doğrulanan satırlar: OpenAlex (imleç, `per_page` ≤ 200, derinlik sınırsız), Semantic Scholar (`offset + limit` ≤ 1.000, yanıttaki `next`), Crossref (`offset` 10.000, imleç dakikalar içinde eskiyor), Scopus (`start`, 5.000), arXiv (`start`, 3 sn bekleme), CORE (`limit`/`offset`). Düzeltilen satır: PubMed'in "9.999" üst sınırı NLM belgesinde yok — ESearch `retmax` 100.000 UID'ye çıkıyor ve `retstart` için sınır yazmıyor; okuma sınırı zaten çok altında, kod değişmedi. IEEE `start_record`'ın 1 tabanlı olduğu belgede yazmıyor (bugünkü kod da 1 gönderiyor). Yapılmadı: tarama hâlâ `max_candidates` ile kesiliyor (dilim 09, K3), yani bulunan binlerce adayın en çok 250'si taranıyor; `unread` hiçbir ekranda yok (dilim 08 / 20), `api.ts` tipleri de yok; eskimiş imleçten kurtulma yok (yeni keşif koşusu gerekir); sayfalar arası kayıt tekrarı ve sağlayıcı sırasının kayması ölçülmedi; canlı sağlayıcı sayfalaması denenmedi, testlerin hepsi SYNTHETIC ve ağsız; canlı istek atılmadı. İncelemede düzeltildi: istek izni her sayfaya sınırlı yeniden denemelerini de tanıyor (önceden her sayfası bir 429'dan sonra başarılı olan uzun okuma `budget_exhausted` ile duruyor ve her sürdürmede yeniden duruyordu); kayıt getirmeyen sayfa imleç taşısa da okumayı bitiriyor. Görülüp bırakılan: `pubmed.py`'de satır içi yorum "9.999" derken docstring sınır belgelenmemiş diyor; Semantic Scholar 1.000'de `next` vermezse satır `provider_cap` yerine `exhausted` yazar (okunmayan yine sayılır). |
| 04d | Blok atamasını modele sordur, kararı kodda tut (SW17) | Kur | Opus · high | tam · yapıldı | kapandı | [dilim](sw-slice04d-model-block-labelling.md) · [prompt](sw-slice04d-prompt.md) | `8fd4140` + inceleme düzeltmesi · D74 | Önkoşul 04a; 04b ve 04c'den bağımsız. Ölçüldü: `.local/sw-block-labelling-2026-09-21/` — 8 soru, 28 ifade, kural 19/28, `deepseek-flash` 28/28, üç koşuda tam kararlı, çağrı başına ~2,3 sn / ~870 jeton. Kuralın edat vetosu bilerek kaldırıldı (öneriye sapma: veto düzeltilen iki vakayı da geri bozuyordu). `skill_package_hash` bu dilimde değişir. Ölçülmeyenler: tek gerçek iddia vakası, tek koşunun yeterliliği, İngilizce olmayan ve bozuk sorular, model hatası yolu. Uygulamadan sonra: `skill_package_hash` `sha256:1f519582…` → `sha256:69606986…`. Dilim dosyasından sapma: "en az bir koşu varsa `block_assignment` model" yerine, model ataması ancak `LABEL_MAJORITY` (2) koşu geldiğinde uygulanır; tek koşuda kural bütünüyle yerinde kalır ve satır `rule` der (tek koşu hiçbir ifadede çoğunluk kuramayacağı için, aksi hâlde sorgu boşalıp koşu `vocabulary_empty` ile duruyordu). Çoğunluk oluşmayan ifade kuralın etiketini yalnızca kayıtta tutar, hiçbir listeye girmez. İfade listesi soru sırasını blok blok taşır (`Extraction` bloklar arası sırayı tutmuyor). Koşuların ne dediği kayıtta kanonik sırada durur, yoksa tekrar aşamasının özeti koşu sırasına bağlanıyordu. Ayrıca yöntem dosyasına zarf alanları (`schema_version` dâhil) satırı eklendi: DeepSeek bağdaştırıcısı çıktı şemasını isteğe koymuyor, kuru çalıştırmanın ilk iki çağrısı `schema_version` olmadan döndü. Güncellenen mevcut test: `test_an_sw_discovery_searches_while_every_model_call_fails` adım sırası (artık `vocabulary` → `vocabulary_labels_1..3` → `protocol`). Yapılmadı: etiketleme arayüzde görünmüyor (dilim 08); `labelling` alanı `run_steps` görünümünde taşınmıyor, yalnızca saklı adım çıktısında; üç çağrı `max_model_calls`'ı yiyor, tarama bütçesiyle ilişkisi dilim 09'a (K3) kalıyor; canlı bağlantıda model hatası yolu denenmedi. İncelemede düzeltildi: aramaya hiçbir geçit terimi bırakmayan etiketleme (koşular hiçbir ifadede anlaşamıyor ya da her ifadeyi iddia / `not_a_term` sayıyor) artık uygulanmıyor, kuralın bütün ataması kalıyor ve kayıt `fallback = labelling_unsearchable` diyor; önceden koşu `vocabulary_empty` ile duruyordu ve tek çıkışı, ekranı olmayan `key_terms` idi. İncelemede görülüp bırakılan: iki koşu başarısız olunca üçüncü çağrı artık çoğunluk kuramaz ama yine atılıyor (asılı kalan bir modelde bir zaman aşımı daha; iki mevcut test üç çağrı bekliyor); duraklatılıp sürdürülen koşu başarısız etiketleme adımını yeniden çağırıyor (bütçeden yer). |
| 05 | Derleme işareti, eksik özet, sürüm bağlantıları | Kur | Opus · high | tam · yapıldı | kapandı | [dilim](sw-slice05-survey-flag-abstract-lookup-links.md) · [prompt](sw-slice05-prompt.md) | `aa482c9` + inceleme düzeltmesi · D77 | Task 1–8 yapıldı, bölünme kullanılmadı. Migration `0043`, karar `D77`. **Canlı sayım (`.local/sw-abstract-lookup-2026-09-21/`):** kuantum havuzunda 105 özetsiz DOI'den S2 30'unu, Crossref 20'sini doldurdu (55 kaldı, %48; 76 istek, 13 sn); paket havuzunda 122'den S2 10'unu, Crossref 3'ünü doldurdu (109 kaldı, %11; 114 istek, 1 yeniden deneme, 35 sn) — **ikinci kaynağın doldurma oranı konuya göre dört kat değişiyor ve paket konusunda düşük**. 302 arXiv DOI'li kayıttan 159'una S2 başka bir DOI adlandırdı. 23 `extended_version` çiftinin yalnızca 6'sında S2 iki DOI'yi de tanıyor ve altısını da **iki ayrı makale** sayıyor: dilim 20'ye kanıt yok. **Yazıldığı gibi yapılamayan:** yeni satır 12b, `test_record_links.py`'deki iki mevcut beklentiyi öne geçiyor — `work_already_has_published` yerine `external_link_names_other_doi` yazılıyor (hüküm aynı: `related_suspected`, `merged=0`); SW6.5 koruması `test_external_links.py`'de sınanıyor. Fikstür değişiklikleri: `test_search_paging.py`'nin Crossref sahtesi artık DOI sorgusuna 404 veriyor. **Açık kalan:** tohum havuzu kurulmuyor (dilim 15), işaretler ve bağlantılar hiçbir ekranda yok (dilim 08/20); başlık derlemesi `unresolved` ve görünmüyor; sorunun kendi sözcüğünü düşürme kuralı ölçülmedi ve çoğul biçimi tutmuyor ("not surveys" → "survey" listede kalır); `not_found` bir daha sorulmuyor; `MAX_LOOKUP_REQUESTS` elle seçildi; çoktan birleşmiş aykırı çift yalnızca sayılıyor; kendi özeti olmayan baş, başka sürümünün özeti olsa da taranmıyor (dilim 09); S2 belgesi parti yanıtının sırasını ve bilinmeyen kimliğin karşılığını söylemiyor, bu yüzden uzunluğu tutmayan yanıt reddediliyor. İncelemede düzeltildi: soru revize edilip başlık sözcüğü sorunun konusu olunca eski `survey_title_word` kararı yerinde kalıyor ve kaydı taramadan tutmaya devam ediyordu, artık `abstract_not_proposed` ile kapanıyor; `external_links` adımı yalnızca ikinci kaynağın doldurduğu özetleri değil koşunun bütün kayıtlarını yeniden bağlantı kuralından geçiriyordu; `held_from_screening` 2.000 adayda 1,1 sn sürüyordu (0,24 sn oldu). İncelemede canlı sınandı (1 istek): S2 partisi yinelenen kimliğe kimlik başına bir yanıt veriyor, yani aynı arXiv DOI'sini taşıyan iki sürüm partiyi bozmuyor. Görülüp bırakılan: dış bağlantı, yazarlar hiç uyuşmasa da ön baskı + yayımlanmış çifti birleştiriyor (SW6.4 öyle diyor; S2'nin yanlış bağlaması kullanıcının geri almasına kalır, geri alma ekranı yok); `contradicted_merges` araştırmanın bütün bağlantılarını dolaşıyor (gerçek havuzda birkaç yüz satır). |
| 06 | Ölçüt, parçaları ve ipucu ifadeleri önerisi | Kur (ölçülmedi) | Fable · high (istem düzeltmesi, yapıldı), Opus · medium (kod) | tek başına · yapıldı | kapandı | [dilim](sw-slice06-criterion-proposal.md) · [prompt](sw-slice06-prompt.md) | `db965af` + inceleme düzeltmesi · D78 | Migration yok, karar `D78`; `skill_package_hash` değişir. **İstem düzeltmesi plan sohbetinde yapıldı** (`.local/sw-criterion-prompt-fix-2026-09-21/`, 30 çağrı, `deepseek-flash`): yeni istem ortamı aranan şeyden ayırıyor; beş soruda (ikisi hiç denenmemiş) 15 koşunun 15'i ve beş uzlaşı listesinin beşi aranan şeyi taşıyor (eski istemde paket boyutu sorusunda 40 ifadenin 0'ı). **Bedeli:** kuantum referansında kalan ifadelerin pasaj sıralaması 53 makalede 48 → 37 (beklenti en az 42 idi, tutmadı; tek koşular 46 / 20 / 24). İzin verilen tek ek düzeltme kusuru yine gideriyor ama 27 veriyor; ürüne ilki girer. İfadeler bu dilimde hiçbir şeyi sıralamıyor, bu yüzden dilim durmuyor; bulgu dilim 11'in başlangıç noktası. Plan kararı: ölçüt cümlesi ve parçalar uzlaşıya en yakın tek koşudan gelir (serbest metin oylanamaz), ifadeler ve dışlama sözcükleri 2/3 oyla. **Uygulandı (21 Eylül 2026):** yeni sözleşme `criterion-proposal.schema.json`, yöntem dosyası `references/criterion-proposal.md`, `workflow/criterion.py` (`norm`, `consensus`), `flow._criterion`, `store.frozen_criterion`, protokolün dört ölçüt alanı + ayrı `criterion_origin` + `thresholds.criterion`, tekrar aşaması `criterion`. Migration yok. `skill_package_hash` `sha256:69606986…` → `sha256:4d84910c…`. **Canlı kuru çalıştırma** (`.local/sw-criterion-dry-run-2026-09-21/`, 6 çağrı, `deepseek-flash` efor high, ayrı `DEIXIS_DATA_DIR`, **0 sağlayıcı isteği**): her iki soruda da aranan şey korundu (`sought_term_in_criterion` = `True`), 6 çağrının 6'sı şemayı ve sözleşmeyi ilk denemede geçti. Paket sorusu: 4 parça, 15 ifade, taban koşu 3, çağrılar 9,3 / 23,8 / 16,6 sn; ölçüt cümlesi paket boyutunu adlandırıyor. Kuantum sorusu: 4 parça, 30 ifade, taban koşu 1, 11,5 / 17,6 / 19,9 sn. **Yazıldığı gibi yapılamayan / sapmalar:** (1) üç efor ön ayarının `max_model_calls` değeri üç arttı (quick 6→9, standard 15→18, detailed 20→23; **incelemede daraltıldı:** ön ayarlar eski değerine döndü, üç çağrı yalnızca `sw` keşif koşusuna ekleniyor), yoksa `quick` bir `sw` koşusu taramaya hiç ulaşamıyor ve `test_an_sw_discovery_searches_while_every_model_call_fails` ile `test_the_request_allowance_covers_the_second_round_and_its_retries` `model_call_failed` yerine `budget_exhausted` ile duruyordu — ön ayarların tarama/yanıt payı böylece dilim öncesiyle aynı kaldı; (2) adım çıktısına dilim dosyasının saymadığı `protocol_revision` alanı eklendi (dondurulmuş ölçütün hangi revizyondan alındığı); (3) `_criterion` sözcük dağarcığını yalnızca `sought_term_in_criterion` kaydı için okuyor. **Güncellenen mevcut testler:** adım sırası yüzünden `test_an_sw_discovery_searches_while_every_model_call_fails`; `test_the_frozen_protocol_names_the_origin_of_every_block_and_is_the_same_on_a_second_build` yeniden kurulumda artık `criterion=` alıyor (beklenti değişmedi). **Yapılmadı / açık:** onay ve düzeltme ekranı yok (dilim 08); ölçüt hiçbir ekranda görünmüyor ve `run_steps` görünümünde taşınmıyor, yalnızca saklı adım çıktısında ve protokolde; ifadeler hiçbir şeyi sıralamıyor (dilim 11) ve dışlama sözcüklerini kimse okumuyor (dilim 09); kuantum referansındaki 48 → 37 sıralama düşüşü açık ve nedeni bilinmiyor; ölçüt cümlesi ve parçalar tek koşudan geliyor; ölçüt kalitesi ürün içinde ölçülmedi (dilim 24); ölçüt adımının üç çağrısı `max_model_calls`'a sayılıyor, tarama bütçesiyle ilişkisi hâlâ dilim 09'da (K3). |
| 07 | Kayıt düzeyinde sıralama | Kur | Opus · high | tek başına · yapıldı | kapandı | [dilim](sw-slice07-record-ranking.md) · [prompt](sw-slice07-prompt.md) | `4747a42` + inceleme düzeltmesi · D79 | Task 1–6 yapıldı. Migration `0044` (`record_references` + `source_versions.references_read`), karar `D79`; `skill_package_hash` **değişmedi** (`sha256:4d84910c…`). Yeni model çağrısı ve yeni sağlayıcı isteği yok; tek yeni ağ işi, gömmenin `sw`'de taramadan önce ve bütün havuzla koşması. Yeni `workflow/ranking.py` (saf işlevler + `rank_records`), `flow._ranking`, `decisions.latest_ranking` / `ranking_order`, `store.source_similarities`, `candidates` satırına `proposed`, protokolde `signals` ve `thresholds.ranking`, tekrar aşaması `record_ranking`. **Ağsız yeniden oynatma** (`.local/sw-ranking-replay-2026-09-21/`, beklenti önceden donduruldu, 1.369 gerçek kayıt, 0 istek, 0 model çağrısı): doğrulanmış tohum yok, üç sinyal; 20 pozitifte **ortanca 145,5**, ilk 100'de 8, ilk 200'de 12, en kötü 544 — beklenti 100–160 aralığı ve BM25'ten kötü olmaması idi, tuttu (`fuse.py` `rrf_no_tfidf` 121 / 9 / 12; ürünün kendi BM25'i tek başına 184,75 / 6 / 10). Referans listesi olmayan kayıt payı %47,6. Saf hesap 0,49 sn, bu yüzden `asyncio.to_thread` **eklenmedi**. Referans yazımıyla en yavaş 200'lük sayfa 0,72 sn (yazımsız 0,60 sn), 04c'nin 1 sn sınırının altında. **Yazıldığı gibi yapılamayan / sapmalar:** (1) `_ranking` dilim dosyasındaki `more` yerine genişleme adımının saklı çıktısındaki terimleri okuyor (sorgu metninden ifade ayıklamamak için); (2) gömmede saklı benzerliği olmayan kayıt, kuyruğun ortak sırası 50'nin altına düşse de kurtarılmıyor (SW8.2: gömmenin yetkisi yok); (3) sinyalin koşmama nedenleri için dilim dosyasında adı geçmeyen iki kod eklendi: `embedding_off` ve `no_stored_similarity`; (4) `availability` adında küçük bir saf yardımcı eklendi (dilim dosyası kuralı anlatıyor, işlevi adlandırmıyor). **Güncellenen mevcut testler:** `test_only_an_sw_protocol_carries_the_record_identity_thresholds` (`sw` gövdesi `thresholds.ranking` ve `signals` kazandı; `legacy` beklentisi değişmedi) — adım sırası yüzünden güncellenen test **yok**, `test_an_sw_discovery_searches_while_every_model_call_fails` yalnızca ilk dokuz adımı sayıyor ve sıralama aramalardan sonra geliyor. **Açık kalan:** `max_candidates` kesmesi duruyor ve artık bu sıraya göre kesiyor (dilim 09, K3); sinyal verimi raporlanmıyor (dilim 19); sıra, kurtarma etiketi ve referans payı hiçbir ekranda yok (dilim 08 / 20), `views.py`'ye dokunulmadı; ortak atıf yok ve referans listesi yalnızca OpenAlex'ten; doğrulanmış + kod tohumu karışımı, sözcük başı eşleşmesi, özetsiz kaydın BM25'te başlıkla puanlanması ve sürümler arası birleşim ölçülmedi; `_source_similarity` hâlâ kısmi sonuç saklamıyor ve 429'da beklemiyor (dilim 21); canlı istek atılmadı, canlı servise ve ürün veritabanına dokunulmadı. İncelemede düzeltildi: iki tarama partisi arasında duraklatılıp sürdürülen koşu, ilk partinin kayıtları öneri aldığı için listeyi yeniden diziyor, parti anahtarları başlangıç yerine bağlı olduğu için sıranın bir parti boyu kaydı hiç taranmıyordu; koşunun kendi taradığı kayıt artık yerinde kalıyor. **Aynı hata `legacy` akışta da vardı ve bu dilimden eskiydi** (ölçüldü: 120 aday, üç çağrı, 40 kayıt taranmadan `completed`); sahibin kararıyla aynı gün ayrı bir commit'te düzeltildi: `legacy` listesi de koşunun kendi taradığı kaydı yerinde bırakıyor, aday sırası başka hiçbir durumda değişmiyor. Görülüp bırakılan: `_embedding_model` ayarı her donduruşta canlı okuyor, yani koşu ortasında değişen ayar genişleme revizyonunda başka model adlandırır; `save_ranks` ile `finish_step` ayrı işlemler (arada çökme eski satırları bırakır, sıra yine tutarlı okunur). |
| 08a | Protokol onayı, arka uç (duraklama, düzeltme paketi, rota, görünüm) | Kur | Opus · high | iki inceleme · yapıldı | kapandı | [dilim](sw-slice08a-protocol-approval-backend.md) · [prompt](sw-slice08a-prompt.md) | `1af3bcf` + inceleme düzeltmesi · D80 | Plandaki dilim 08 üçe bölündü (21 Eylül 2026). Migration yok, karar `D80`; yeni model çağrısı yok, `skill_package_hash` aynı kalır. Plan kararları (dosyada adıyla): koşu ölçüt adımından sonra, protokol dondurulmadan önce `protocol_approval_needed` ile durur; **sayım sınamaları onaydan önce koşar** (ana planın kabul koşulu "arama isteği"ne daraltıldı); düzeltilmiş dağarcık elle yamanmaz, `build_vocabulary`'den yeniden kurulur ve okunmuş sayımlar yeniden sorulmaz; ağ işi rotada değil işçide; onay soru + yönlendirme + `key_terms` başına bir kez sorulur, sonraki koşu düzeltmeleri yeniden uygular; katılımsız koşu için `Settings.protocol_approval = as_proposed` (mevcut `sw` testlerinin kurulumu buna çevrilir, protokolde kullanıcı onayı gibi görünmez). **Sahip görmeli:** 08a ile 08b arasında `ask` modundaki bir `sw` araştırması yalnızca API ile sürdürülebilir. **Uygulandı (21 Eylül 2026).** Task 1–6 yapıldı. Migration yok, karar `D80`, `skill_package_hash` **değişmedi** (`sha256:4d84910c…`), yeni model çağrısı ve yeni sözleşme yok. Yeni: `backend/deixis/workflow/approval.py` (saf: `check_edits`, `edited_extraction`, `apply_criterion`, `applicable`, `canonical_edits`, `block_origins`, `proposal_hash`, `exclusion_words_in_question`), `flow._approval` + `_approved_vocabulary`, `store.approval_step` / `approvals_of` / `submit_approval`, rota `POST /api/runs/{id}/protocol-approval`, `views.approval_view`, `Settings.protocol_approval`, protokol gövdesinde `approval`, tekrar aşaması `protocol_approval`, 64 yeni test (`tests/test_protocol_approval.py`, `tests/test_approval_flow.py`). **Sayım önbelleği:** sarmalayıcı değil, `build_vocabulary`'ye `known` argümanı — sarmalayıcı önbellekten gelen yanıtı da `MAX_PROBES` bütçesinden düşürüyordu, yani yeniden kurulan dağarcık kullanıcının eklediği terimi hiç sayamadan bütçeyi bitiriyordu. **Sapmalar:** (1) dilim dosyası "düzeltmesiz onayda ağ isteği sıfır" derken terim silmeyi de sayıyor, ama silme geçit sorgusunun metnini değiştiriyor ve o metni hiçbir sınama okumadı — silme ve taşıma yeni geçit sorgusunun sınamasını ödüyor, terim eklenmeyen onay hiç istek atmıyor; (2) önceki onay yeniden uygulanırken yalnızca terim işlemleri taşınıyor, ölçüt düzeltmesi taşınmıyor: ölçüt zaten `frozen_criterion`'dan onaylanmış hâliyle geliyor ve ikinci kez düzeltmek anlaşılanı yeniden yazardı; (3) `approval.proposal_rows` eksik yan listeyi boş sayıyor (mevcut `test_protocol_record.py` fikstürleri kısmi bir dağarcık şekli veriyor); (4) adım çıktısına dilim dosyasının saymadığı `approval` (protokol gövdesinin kaydı) ve `earlier_approval_step_id` alanları eklendi. **Kurulumu `as_proposed`'a çevrilen mevcut test dosyaları:** `test_vocabulary_flow.py`, `test_expansion_flow.py`, `test_ranking_flow.py`, `test_lookup_flow.py`, `test_search_paging.py` (ve `app_for`'u ödünç alan `test_criterion_flow.py`, `test_vocabulary_labels.py`). **Adım sırası yüzünden güncellenen testler:** `test_an_sw_discovery_searches_while_every_model_call_fails`, `test_three_proposals_reach_the_protocol_before_the_first_provider_request`. **Yeniden kurulumda `approval=` alan testler** (beklenti değişmedi): `test_the_frozen_protocol_holds_the_concept_blocks_and_is_the_same_on_a_second_build`, `test_the_frozen_protocol_names_the_origin_of_every_block_and_is_the_same_on_a_second_build`, `test_an_accepted_phrase_freezes_a_second_revision_and_leaves_the_first_alone`. **Açık kalan:** ekran yok (08b), yani `ask` modundaki bir `sw` araştırması yalnızca API ile sürdürülebilir; onayı yeniden açmanın yolu yok (soru / yönlendirme / `key_terms` değiştiren kapsam revizyonu dışında); önceki onay, onu kimin verdiğine bakılmaksızın yeniden kullanılıyor (`as_proposed` ile ölçülüp `ask`'e geçilen araştırma sorulmaz); yeniden uygulanan işlem artık var olmayan ifadeye dokunuyorsa sessizce atlanıyor ve yalnızca `skipped_edits`'te duruyor; kullanıcının eklediği terimin doğruluğunu yalnızca sayım sınıyor ve yazdığı ifadeler hiçbir koşuda oylanmadı; koşullu model terim önerisi (SW2.5) yok (08c); düzeltmelerin kaliteye etkisi ölçülmedi (dilim 24); canlı istek, model çağrısı ve kuru çalıştırma yok. İncelemede düzeltildi: (1) ilk koşuda model kapalıyken ölçütsüz onay verilmişse, sonraki koşunun modelden ilk kez aldığı ölçüt "önceki onay" sayılıp kullanıcıya hiç gösterilmeden protokole giriyordu; artık kimsenin görmediği ölçüt yeniden sorduruyor. (2) Önerisi aranamayan (boş ya da fazla geniş) koşu onay adımına hiç ulaşmadan duruyordu, yani 08b kartının düzeltebileceği tek durumda kart açılamıyordu; `ask` modunda öneri artık kullanıcıya gidiyor, katılımsız koşu eskisi gibi hemen duruyor. Görülüp bırakılan: (1)'de yeniden sorulan kullanıcı önceki terim düzeltmelerini yeniden yapmak zorunda (öneri taze dağarcıktır); Opus incelemesinin üç notu — `approval.py`'de `terms` liste değilse öbür hatalar söylenmiyor, `research_view` her koşu için bütün onay çıktısını okuyor, `views.py`'deki `"rule"` varsayılanı ölü — 08b ile düzeltilebilir. |
| 08b | Protokol onayı, arayüz (onay kartı, üç duraklamanın çıkış yolu, Playwright H) | Kur | Opus · medium | tek başına · yapıldı | kapandı | [dilim](sw-slice08b-protocol-approval-ui.md) · [prompt](sw-slice08b-prompt.md) | `85418fd` + inceleme düzeltmesi · D80 (Limits) | Yeni D numarası yok (D80'in Limits'i güncellendi). **Uygulandı (21 Eylül 2026).** Task 1–5 yapıldı. Yeni: `apps/web/src/ProtocolApproval.tsx`, `apps/web/e2e/protocol-approval.spec.ts` (senaryo H, yedi durum). Değişen: `api.ts` (`RunApproval` / `ApprovalSide` / `ApprovalTerm` / `ApprovalCriterion` / `ProtocolEdits` tipleri, `approveProtocol`, 422'nin hata listesini taşıyan `ApiError.errors`, `reviseScope`'a `key_terms`, dilim 02'nin `code_rule` açığı kapandı), `Transcript.tsx` (kart koşu öğesinin içinde; `key_terms_needed` notunda revizyon formuna götüren düğme), `ResearchView.tsx` (onay bekleyen koşuda düz **Sürdür** yok; `RevisionForm`'a yalnızca `sw`'de görünen İngilizce anahtar terim alanı), `labels.ts` + `i18n.ts` (dört duraklatma cümlesi, blok adları ve notları, köken rozetleri, düşme nedeni, onaylayan; hepsi EN+TR), `workspace.css`. **Arka uca dokunulmadı:** izinli tek değişiklik olan `views.research_view`'ın `search_workflow` ve `key_terms` alanları zaten görünümde (`store.scope` ikisini de veriyor, `scope_view` hepsini taşıyor), dolayısıyla ne kod ne test eklendi. **Sapmalar:** (1) `playwright.config`'e ikinci `webServer` girdisi eklenmedi — mevcut kurulumda A–G de `webServer` kullanmıyor, sunucuyu spec'in kendisi başlatıyor; H de aynı kalıpla kendi sunucusunu (`DEIXIS_SEARCH_WORKFLOW=sw`, `DEIXIS_PROTOCOL_APPROVAL=ask`, port 8789, kendi veri klasörü) başlatıyor ve A–G'ye dokunulmadı. (2) Görünüm terim kökenini ve blok kökenini yalnızca aranan iki blok için taşıyor; yan liste ifadelerinde (outcome / claim / exclusion) rozet gösterilmedi, uydurulmadı. (3) `approval` kaydındaki `exclusion_word_in_question` görünümde olmadığı için uyarısı da yok. (4) Ölçüt parçasının tanımı `input` değil `textarea`: tek satırda kesiliyordu. (5) Kart başlığı `<header>` değil `<div>`: kabuğun `header{height:77px;display:flex}` kuralı dar ekranda metni üst üste bindiriyordu (390 px ekran görüntüsünde görülüp düzeltildi). **Açık kalan:** ölçütsüz koşunun kartı (model kapalıyken) ve gönderim sınanırken kilitlenen kart yalnızca elle görüldü, testi yok — fikstür modeli hiç düşmüyor ve koşu kilidi görecek kadar yavaş değil; taslak düzeltme yalnızca bileşen durumunda, sayfa yenilenince kayboluyor; arka ucun reddettiği (`vocabulary_empty`) gönderim `submitted` olarak duruyor ama yenilemeden sonra karta geri çizilmiyor; kartın anlaşılırlığı ve 1–2 dakikalık bekleme kimseyle sınanmadı; koşullu model terim önerisi yok (08c); düzeltmelerin kaliteye etkisi ölçülmedi (dilim 24). İkinci fikstür sunucusu (`sw` + `ask`); A–G aynı kaldı (42 Playwright durumu geçti, pytest 1.299 geçti, bilinen 1 başarısızlık). Gösterilmeyen: ikinci turun veri terimleri ve verim (dilim 20). İncelemede düzeltildi: (1) onay bekleyen koşuda zaman çizelgesi "Search plan · Waiting" diyordu, oysa koşu dağarcığı saymış, altı model çağrısı yapmış ve kullanıcıyı bekliyordu (`sw`'nin plan adımları hiçbir faza bağlı değildi); artık plan fazına bağlı ve beklerken kehribar. (2) Kart iki yerde yapının yapmadığı şeyi söylüyordu: "dışlanan sözcükler başlığında geçen kaydı dışarıda tutar" (bugün hiçbir kod okumuyor, dilim 09) ve ölçütün dışlama başlık sözcükleri için aynısı; metinler "protokolle saklanır, henüz kullanılmıyor" oldu (EN+TR). (3) Kart "sonuç terimleri kayıtları sıralar" diyordu, ama kullanıcının karta eklediği sonuç terimi BM25'in okuduğu sözcüklere girmiyordu; artık giriyor (sorudaki terimler zaten içindeydi, düzeltmesiz koşuların sırası değişmez). |
| 08c | Model terim önerisi (SW2.5), yalnızca kullanıcı isteyince: onay kartındaki düğme modelden arama terimlerinin başka adlarını ister, her öneri sayım sınamasından geçer, kökeni `model` görünür ve kullanıcı eklemeden hiçbiri sorguya girmez | Kur (ölçülmedi) | Opus · high | tek başına · yapıldı | kapandı | [dilim](sw-slice08c-model-term-suggestions.md) · [prompt](sw-slice08c-prompt.md) | `D82` | **Uygulaması dilim 09 commit'lendikten sonra başlar** (satır 09 en az `uygulandı, inceleme bekliyor`): iki dilim aynı sözleşme, yöntem paketi, fikstür ve bütçe dosyalarına dokunur ve ikisi de `skill_package_hash`'i değiştirir. 09'un önkoşulu değildir. Migration yok, karar sıradaki boş numara (beklenen `D82`). **Sahibin kararı (21 Eylül 2026): otomatik tetik yok**; "ilk tur zayıf" eşiği tanımlanmadı, dilim 24'ün ölçümüne bırakıldı. Plan kararları (dosyada adıyla): model sorgu değil terim listesi önerir ve her öneri karttaki bir terimin başka adı olmak zorundadır (`synonym_of` izin listesinden; blok koddan türer); tek çağrı, onarımsız, oylama yok (sayım ve kullanıcı iki süzgeç; 2/3 oyu seyrek ama doğru adı elerdi); iddia ya da dışlama ifadesi taşıyan öneri kodla düşer; öneri adımı yalnızca ifade sayımını okur, kök / geçit kararı onaydan sonra `build_vocabulary`'de; terim kökeni sunucuda türetilir (`add` işleminin biçimi değişmez); öneri istenmeyen koşunun protokol gövdesi ve özeti aynı kalır; `ready` dönen öneri yeniden istenmez, yalnızca `failed` yinelenir. **Ölçülmedi:** yöntem dosyasının istemi plan sohbetinde yazıldı ve hiçbir modelle denenmedi; `MAX_SUGGESTED_TERMS = 12` elle seçildi. **Kurulmayan:** SW2.5'in "ilk tur zayıfsa" yarısı ve "doğrulanmış bir pozitifte geçmeyen terim kalmaz" kuralı (doğrulanmış pozitif dilim 12'den önce yok). Bölünme noktası: Task 5–6 (kart ve Playwright) ayrı bırakılabilir. **Uygulandı (21 Eylül 2026).** Task 1–7 yapıldı, **bölünme noktası kullanılmadı**. Migration yok, karar `D82`; `skill_package_hash` **değişti** (`sha256:cd1db33c…` → `sha256:8f0e6cfb…`). Yeni: `contracts/research/term-suggestions.schema.json`, `methods/deixis-research/references/term-suggestions.md` (metni dilim dosyasından **değiştirilmeden** alındı), `backend/deixis/workflow/suggestions.py` (saf: `anchors`, `target`, `screen`, `known_counts`, `model_phrases`), `flow._term_suggestions` + `_suggested`, `store.suggestion_steps` / `request_term_suggestions`, rota `POST /api/runs/{id}/term-suggestions`, `views._suggestions_side`, `apps/web/src/ProtocolApproval.tsx`'te **Modelden terim öner** bölümü, `tests/test_term_suggestions.py` (31 durum) ve `tests/test_suggestion_flow.py` (24 durum), tekrar aşaması `term_suggestions`, dört sözleşme fikstürü, Playwright H'ye dört durum. **Kanıt sınırı:** `sw` protokolünde bir terimin kökeni `model` olabilir; `approval` gövdesi yalnızca öneri istendiyse ya da taşındıysa `suggestions` kazanır; öneri istemeyen koşunun gövdesi ve özeti bu dilimden önceki değerdedir (testi: `test_a_run_that_never_asked_keeps_its_protocol_body_and_opens_no_step`). **Sapmalar:** (1) dilim dosyasının saymadığı iki mevcut test bütçe formülüne `SUGGESTION_CALLS` terimini aldı (`test_abstract_flow.py::test_the_run_budget_holds_the_abstract_stage_s_calls_on_top_of_the_preset`, `test_criterion_flow.py::test_three_proposals_reach_the_protocol_before_the_first_provider_request`) — dilimin kendi bütçe talimatının kaçınılmaz sonucu; iki testin hükmü (ön ayar dokunulmamış, ekler üstüne biniyor) aynı kaldı; (2) `protocol.py` değiştirilmedi: terim kökenini olduğu gibi kopyalıyor ve `model` değeri kendiliğinden geçiyor, blok kökenini de `approval.block_origins` `user` veriyor; (3) `_approval`'da önceki onay araması öneri taşıma için gövde yazımından önceye alındı (dilim dosyası iki yeri ayrı anlatıyor, kaynak tek); (4) kart satırındaki blok rozeti düz blok adı değil "{blok} bloğuna girer" diyor — düz ad Playwright'ın blok seçicisiyle çakışıyordu; (5) `suggestions.py` adlandırılmamış bir `DROP_REASONS` sabiti taşıyor (nedenlerin kanonik listesi). **Yapılmadı / açık:** otomatik tetik yok ve "ilk tur zayıf" eşiği tanımlanmadı (dilim 24); "doğrulanmış pozitifte geçmeyen terim kalmaz" kuralı yok (dilim 12 / 19); **istem ve iki elle seçilen sayı hiçbir modelle denenmedi** — canlı model çağrısı, sağlayıcı isteği ve kuru çalıştırma yok, yani kaç önerinin sayımı geçtiği, kaçının gerçekten başka ad olduğu ve komşu konuya kayma ölçülmedi; tek çağrı, oylama yok, liste koşudan koşuya değişebilir; sayım bir adın yazında var olduğunu gösterir, aynı şeyi adlandırdığını değil; model kullanıcının taslak düzeltmelerini görmez; öneri yalnızca aranan iki blok için; iptal edilen koşunun önerileri taşınmaz; sayım istekleri `provider_requests` sayacına girmiyor (04a ile aynı); istek sırasında kilitlenen kartın testi yok (fikstür modeli yarış açmayacak kadar hızlı), elle görüldü. Sınamalar: pytest 1.425 geçti (bilinen 1 başarısızlık; temel 1.368), web build temiz, lint 17 uyarı (temelle aynı), Playwright A–G 35 + H 12 = 47 durum geçti. Bakılan ekran görüntüleri: `/tmp/deixis-acceptance/H-suggestions-{ready-desktop,ready-mobile-dark,failed-desktop,failed-mobile-dark,approved-desktop}.png`; ilk bakışta hata durumunda **Yeniden dene** ile **Modelden terim öner** yan yana çıkıyordu, düzeltildi (artık düğme yalnızca hiç sorulmamış kartta). |
| 09 | Özet taraması v2 | Kur | Opus · high | tam · yapıldı | kapandı | [dilim](sw-slice09-abstract-screening.md) · [prompt](sw-slice09-prompt.md) | `D81` | Migration yok, karar `D81`; `skill_package_hash` **değişti** (`sha256:4d84910c…` → `sha256:cd1db33c…`). K3 kararı uygulandı: model sıranın başındaki 40 / 100 / 300 işi okur (efora göre), çağrı başına 20 kayıt, iki koşu; okunmayan `abstract_not_read` ile kalır, ikinci keşif koşusu kaldığı yerden okur. **Uygulandı (21 Eylül 2026), iki commit.** Task 1–4 ve 6–8 birinci commit'te (`fb88d8d`); **Task 5 (eşzamanlı gönderim) ikinci commit'te** (bu satırın yazıldığı commit): (parti, koşu) çağrıları artık `_table_fill`'in gönderim döngüsünden geçiyor — döngü `flow._send_through_limiter`'a çıkarıldı ve ikisi de onu kullanıyor, `_table_fill`'in davranışı ve testleri değişmedi. Aynı anda uçuşta en çok `deps.limiter.limit` çağrı olur (varsayılan 6), her gönderimden önce `_checkpoint` koşar, parti iki koşusu da döndüğünde olay döngüsünde kısa işlemle kapanır, hız sınırı yanıtı `limiter.reduce()` yolundan geçer. Bütçe eşzamanlılıkta da tutuyor: gönderilmiş ama henüz model oturumunu açmamış çağrılar `_model_calls_left`'e ayrıca sayılıyor, böylece hiçbir gönderim `max_model_calls`'u aşmıyor ve parti yarım gönderilmiyor. Yeni: `tests/test_abstract_concurrency.py` (5 durum: limit sınırı ve kararların sıralı gönderimle aynılığı, duraklatma, bütçe, geçersiz koşu, hız sınırı). Yeni: `backend/deixis/workflow/abstract_stage.py` (saf: `code_outcome`, `should_write`, `reading_version`, `read_plan`, `model_calls`, `verified`, `combine`, `proposals_of`), `contracts/research/abstract-screening.schema.json`, `methods/deixis-research/references/abstract-screening.md`, `tests/test_abstract_stage.py` (36 durum) ve `tests/test_abstract_flow.py` (21 durum). Değişen: dört yeni gerekçe kodu (`notice_record`, `artifact_of_paper`, `abstract_not_read`, `runs_agree_unresolved`), `flow._abstract_stage` + `_abstract_code_stage` + `_close_abstract_batch` (eski tarama döngüsü yalnızca `legacy` dalında), `ranking.block_forms` / `blocks_in` (tek eşleyici, `block_scores` bunları kullanıyor), `decisions.staleness_key`, `contracts` (yeni görev, `_check_abstract_screening`, aday tutamakları `cnd_C…`), `step-input.schema.json` (`screening_target`), `protocol.thresholds.abstract_screening`, `api/app.py` bütçesi, `determinism_stages.abstract_stage`, `Transcript.tsx` + `labels.ts` + `i18n.ts`. **Kanıt sınırı:** `sw` koşusunda model artık seçim yazmıyor, kod yazıyor; anlaşılmış `out_of_scope` seçimi `excluded` + `code_rule` yapıyor; **özetten hiçbir iş dahil edilmiyor**, yani dilim 12'ye kadar `sw` araştırması yanıt için kullanıcının elle dahil etmesini bekler. **Sapmalar:** (1) `should_write` eskimiş kararı aynı kodla da yeniden yazıyor — yazmazsa kayıt sonsuza dek eskimiş görünür ve her koşuda yeniden okunur; (2) `CAPABILITIES.supported_tasks`'e yeni görev eklenmedi, çünkü eklemek `legacy` dâhil her adım girdisini değiştirirdi; (3) dilim dosyasının saymadığı üç test dosyası, model kapalıyken koşunun artık taramada durmak zorunda olmaması yüzünden güncellendi (`test_search_paging.py` dört durum ve `test_expansion_flow.py` bir durum `reached_screening` yardımcısına çevrildi, `test_criterion_flow.py` bütçe ve duraklama beklentisi, `test_protocol_record.py` kurgusal kapsamlara `effort` eklendi). **Açık kalan:** sayılar, gerekçe kodları ve "okunmadı" hiçbir ekranda yok (dilim 20) ve ana ekrandaki derinlik açıklaması hâlâ `legacy` sınırlarını anlatıyor; konudan bağımsız istemin dar ve kararsız okuması (100'de 77 eşleşme, 17 anlaşmazlık) biliniyor ve düzeltilmedi; etiket doğruluğu ölçülmedi (insan etiketi yok); bağlantısız ek ürün normal kayıt gibi taranıyor; işin tek sürümü okunuyor; genişleme terimlerinin aday sayısını ne kadar şişirdiği ölçülmedi; canlı istek, model çağrısı ve kuru çalıştırma yok. Sınamalar: pytest 1.368 geçti (bilinen 1 başarısızlık; Task 5'ten önce 1.363), web build + lint temiz, Playwright A–H 42 durum geçti (build, lint ve Playwright birinci commit'in sonucu: Task 5 `apps/web`'e dokunmadı). Task 5'te beklentisi gevşetilen tek mevcut test: `test_ranking_flow.py::test_a_run_paused_between_two_screening_batches_screens_the_next_places_of_the_order_when_it_resumes` artık partileri sırasız karşılaştırıyor — düşen bağlantının maliyeti olan çağrı, duraklama sırasında uçuşta olan partilerden sonra yeniden gönderiliyor; hangi kayıtların hangi partide ve kaç kez okunduğu beklentisi aynı kaldı. İncelemede düzeltildi: (1) eşzamanlı gönderimin bütçe sayacı, sürdürülen koşuda depodan geri okunan çağrıları da "gönderilmiş" sayıyordu; bütçe tam yetse bile ödenmiş yanıtlar kullanılmıyor, sonraki partiler `abstract_not_read` kalıyor ve koşu `completed` oluyordu (artık yalnızca yapılacak çağrı sayılıyor). (2) Aranan tek blok varken `both_blocks_missing` "o tek blok yok" anlamına geliyor ve kod kaydı kapsam dışı sayıyordu; artık iki geçit bloğu da terim taşımıyorsa kod bu yoldan hiçbir kaydı kapatmıyor, model okuyor (SW9.2). Görülüp bırakılan: iki blok da öbek biçiminde sorguya girdiyse, ikisini de başka sözcüklerle anlatan özet kodla kapsam dışı kalır (SW9.2'nin tasarımı, ölçülmedi, dilim 24); kullanıcının elle dışladığı ama aşama kararı olmayan kayıt yine modele gidiyor (zararsız, bir çağrı yeri); işin başı iki koşu arasında değişirse aynı iş başka sürümüyle yeniden okunabilir. |
| 10 | Arka planda tam metin getirme | Kur | Opus · high | tek başına · yapıldı | kapandı | [dilim](sw-slice10-background-fulltext-fetch.md) · [prompt](sw-slice10-prompt.md) | `D83` | Önkoşul 03, 07, 09 (kapandı). Migration `0045` (koşu türü), model çağrısı yok, `skill_package_hash` aynı kalır. **Sahibin kararları (21 Eylül 2026):** (1) "arka plan" ayrı bir koşudur: `sw` keşif koşusu `completed` olunca `fulltext_fetch` koşusu kendiliğinden kuyruğa girer; işçi tek olduğu için o sürerken yanıt / keşif başlatmak isteyen kullanıcı getirmeyi duraklatır — SW10.1'den adıyla sapma (getirme kod aşamasından hemen sonra değil, keşif koşusundan sonra başlar); (2) koşu başına iş sınırı 40 / 100 / 300, gerisi "getirilmedi" sayılır ve sonraki koşu oradan sürer; (3) yalnızca başka sürümün DOI'si doğrulanmış kopyası açıksa kod o sürüm için işin altında satır açar ve PDF'i ona bağlar (D4 korunur, D48 kalıbı), sürümü bilinmeyen kopya kullanıcıyı bekler; (4) arXiv başlık araması ve Europe PMC **kurulmaz** (ölçülen verim 2 / 116 ve 3 / 118, tek konu; Europe PMC dilim 14'e not). Plan kararları (dosyada adıyla): sıra kullanıcının dahil ettiği → aday → `next_step`'i `fulltext_fetch` olan `unresolved`, grup içinde dilim 07'nin sırası; **özeti okunmamış iş getirilmez** (SW10.1'den dar: yoksa tam metin kararı, özeti sonradan kapsam dışı çıkan işi `pending`'de tutardı); plan dondurulur; her yolu yanıt veren metinsiz iş `no_fulltext`, bir yolu yanıt vermeyen iş karar almaz ve yeniden denenir; başka-kopya araması açık bağlantısı hiç olmayan iş için de bir kez koşar (D35'ten geniş, yalnızca bu koşu türünde); kimlik denetimi `match_pdf_to_source`'un taşınmış hâli, `unconfirmed` metni ayırmaz. Dilim 09 incelemesinin iki dersi kısıt olarak dosyada: sürdürülen koşuda depodan geri okunan iş bütçeye / sayaca yazılmaz (sınaması adıyla isteniyor), eşzamanlılık için ikinci düzenek yazılmaz (`_send_through_limiter`; bu dilim sıralı getirir). **Ölçülmedi:** süre, erişim oranı, sürüm etiketi sırası, kimlik denetiminin gücü, iş sınırının bedeli, sıralı getirmenin bedeli — hepsi dilim 24. **Sahip görmeli:** `detailed` eforda getirme kabaca 25 dakika (ölçülmedi) o araştırmayı ve kuyruktaki başka koşuları bekletir; eskimiş tam metin kararının `work_outcome`'da özet kararını gölgelemesi dilim 12'ye yazıldı. **Uygulandı (21 Eylül 2026), tek commit.** Migration `0045` (koşu türü `fulltext_fetch`), karar `D83`; `skill_package_hash` **değişmedi** (`sha256:8f0e6cfb…`), hiçbir model çağrısı, hiçbir sözleşme ve hiçbir yöntem dosyası değişmedi. Yeni: `backend/deixis/workflow/fulltext.py` (saf: `fetch_plan`, `settled_code`, `group_of`, `should_write`, `fetch_budget`), `backend/deixis/documents/identity.py` (`match_pdf_to_source` `api/app.py`'den **taşındı**, yeni `check`), migration `0045`, `tests/test_fulltext_plan.py` (31 durum) ve `tests/test_fulltext_flow.py` (24 durum). Değişen: `flow._fulltext_fetch` + `_fulltext_plan` + `_fulltext_works` + `_fulltext_work` + `_fetch_work_text` + `_fulltext_summary` + `_queue_fulltext_fetch`, `_find_other_copy(other_versions=)`, `acquisition.acquire_for_source(other_versions=)` + `_attach_other_version`, `store.open_lookup_version` + koşu aşaması eşlemesi + `STEP_OUTPUT_KINDS`, `domain/rules.FULLTEXT_WORK_LIMIT`, `config.Settings.fulltext_fetch`, `api/app.StartRun` + bütçe + `legacy`'de 422, `protocol.thresholds.fulltext_fetch`, `determinism_stages.fulltext_plan`, `api.ts` + `labels.ts` + `i18n.ts` + `Transcript.tsx` + `BackgroundJobs.tsx` + `ResearchView.tsx`. **Eşzamanlılık eklenmedi: işler sırayla getiriliyor** (ders 2), ve sürdürülen koşu depodan okuduğu işi hiçbir sayaca yazmıyor, özet saklı adım çıktılarından toplanıyor (ders 1; sınaması `test_a_run_paused_in_the_middle_finishes_the_whole_plan_when_it_is_resumed`, plan boyu sınıra tam eşit). **Kanıt sınırı:** kod, kullanıcı onayı olmadan bir işin altına sürüm satırı açıp PDF bağlayabiliyor — yalnızca DOI'si doğrulanmış ve sürümü beyan edilmiş kopya için; tam metin aşamasına ilk kez karar yazılıyor ve hepsi `unresolved`; başka-kopya araması hiç açık bağlantısı olmayan iş için de bir kez koşuyor (yalnızca bu koşu türünde). **Sapmalar (adıyla):** (1) getirme keşif koşusundan sonra başlıyor, kod aşamasından hemen sonra değil (SW10.1); (2) özeti okunmamış iş getirilmiyor (SW10.1'den dar); (3) sürüm sırası barındıran türe göre değil etikete göre (SW10.3); (4) başka-kopya araması `_acquire_pdf` içinde 403/404'ten sonra **dar** tetikle koşarsa (`pdf_discoveries` dolu olur) sürüm satırı yolu o iş için hiç denenmiyor — dilim dosyasının "henüz yapılmadıysa" kuralı aynen uygulandı; (5) kullanıcının dahil ettiği iş `out_of_scope` olsa da getiriliyor (grup 1 kod kararlarından önce okunuyor, User Authority). **Ölçülmedi:** süre, erişim oranı, sürüm etiketi sırasının verimi, kimlik denetiminin gücü, iş sınırının ve sıralı getirmenin bedeli — canlı istek, indirme ve kuru çalıştırma yok (dilim 24). **Açık kalan:** sayılar ve kodlar hiçbir ekranda yok (dilim 18 / 20); eskimiş tam metin kararı `work_outcome`'da özet kararını gölgeliyor — bir testle **belgelendi**, düzeltilmedi (dilim 12); `unconfirmed` kimlik metni ayırmıyor; taze `no_fulltext` yeniden denenmiyor; başka-kopya isteklerinin `provider_requests` sayacına girmemesi sürüyor. Sınamalar: pytest 1.494 geçti (bilinen 1 başarısızlık; temel 1.432), web build + lint temiz, Playwright A–H 47 durum geçti. Kurulumu `fulltext_fetch="off"`a çevrilen dosyalar: `test_vocabulary_flow.py`, `test_ranking_flow.py`, `test_expansion_flow.py`, `test_lookup_flow.py`, `test_search_paging.py`, `test_abstract_flow.py`, `test_approval_flow.py` (ve kurulumu ödünç alan `test_vocabulary_labels.py`, `test_criterion_flow.py`, `test_abstract_concurrency.py`) ile `tests/acceptance/fixture_server.py`; beklentisi değişen tek mevcut test `test_only_an_sw_protocol_carries_the_record_identity_thresholds` (`sw` gövdesi `thresholds.fulltext_fetch` kazandı). |
| 11 | Ölçüt pasajları | Kur (ölçülmedi) | Opus · medium | tek başına · yapıldı | kapandı | [dilim](sw-slice11-criterion-passages.md) · [prompt](sw-slice11-prompt.md) | `D84` | Önkoşul 06 (kapandı; ifadelerin onayı 08a / 08b). Migration yok, model çağrısı yok, `skill_package_hash` aynı kaldı (`sha256:8f0e6cfb…` önce ve sonra). Yeni: `backend/deixis/workflow/criterion_passages.py` (derleme, puan, `criterion_order`), `tests/test_criterion_passages.py` (12), `tests/test_criterion_passage_flow.py` (21). Değişen: `flow._answer` + yeni `flow._criterion_phrases` (`code:criterion_phrases` adımı), `flow._retrieve` (`patterns` argümanı, iki kota, kalabalık durumda `_two_quota_pages`), `store.STEP_OUTPUT_KINDS`, `protocol.py`, `tests/determinism_stages.py` (yeni `criterion_passages` aşaması), `labels.ts` / `i18n.ts` / `Transcript.tsx`. Dilim dosyasının dediği yapıldı: ifadeler yalnızca `frozen_criterion` ile okunuyor ve eskimiş / yanıtsız satırın beş durumu adıyla sınanıyor; adım koşuda donuyor; `legacy` dalı ve `_cell_passages` değişmedi; geriye örtüşme kurulmadı; ifade puanı hiçbir tabloya yazılmıyor. **Sapmalar (adıyla):** (1) puanlama olay döngüsünde koşuyor, `asyncio.to_thread`'e alınmadı — dilim dosyasının koşulu bir süre ölçümü ister, prompt kural 12 ise bu dilimde süre ölçümünü yasaklıyor; `_retrieve` eşzamanlı (sync) kalsın ve `legacy` yolu bayt bayt korunsun diye basit yol seçildi, **ölçülmedi**. (2) İzin verilen listede adı geçmeyen ikinci bir mevcut test, `test_build_protocol_is_repeatable_and_reads_its_thresholds_from_their_definitions`, aynı adlandırılmış değişikliğin (sw gövdesi `formulation_score_threshold`'u yitirir) doğrudan sonucu olduğu için asgari biçimde güncellendi: eşiği artık `legacy` gövdeden okuyor. (3) `compile_phrases`'in `dropped` satırları ham ifadeyi değil `norm`'dan geçmiş biçimi taşır, yoksa aynı ifadeyi iki yazımla içeren bir liste satır sırasına bağlı olurdu. (4) `criterion.norm` uçlardaki noktalamayı attığı için `c++` tek karaktere inip `too_short` düşüyor; ikinci bir normalleştirici yazılmadı (kural 8), durum testte adıyla duruyor. (5) Adımın sakladığı `phrases` ham protokol satırları değil, tutulan ifadelerin sıralı metinleridir; sürdürülen koşu bunları yeniden derliyor. **Ölçülmedi:** iki kotanın çok kaynaklı 48 pasajlık girdideki etkisi, kota payı (`limit // 4`), kalabalık durumda 1 + 1 bölüşümü, eşiksiz liste, her makalede geçen ifadelerin sırayı bulandırması, İngilizce dışı dillerde desen, puanlamanın süresi — hepsi dilim 24. **Açık kalan:** ifadelerin, düşenlerin ve "ifade yok" nedeninin ekranı yok (dilim 18 / 20), yani kötü bir ifade listesi `sw` yanıtını sessizce zayıflatır; ölçütü olmayan `sw` yanıtı formülasyon kotasını yitirdi; SW12.4'ün öbür üç tüketicisi kurulmadı (tam metin okuması dilim 12, kuyruk ipuçları dilim 16, Marker sayfaları tüketicisiz); tablo hücrelerinin pasaj seçimi değişmedi; eşik ya da ters belge sıklığı ağırlığı yok. İncelemede düzeltildi (22 Eylül 2026): kalabalık seçim limiti doldurmadığında `sw` dalında bir kaynak 6 yerine 8 pasaj verebiliyordu; sayaç artık kalabalık dalın verdiklerini sayıyor. `legacy` dalındaki aynı kusur bayt bayt bırakıldı ve D84 Limits'e yazıldı. |
| 12 | Tam metin kararı | Kur (ölçülmedi) | Grok 4.7 (sahibin kararı) | tam · yapıldı | kapandı | [dilim](sw-slice12-fulltext-adjudication.md) · [prompt](sw-slice12-prompt.md) | `3ab18a2` · D85 | Önkoşul 02, 10, 11 (kapandı). Migration `0046` (koşu türü), yeni sözleşme ve yöntem dosyası, `skill_package_hash` **değişir**. **Sahibin kararları (22 Eylül 2026):** (1) okuma ayrı bir koşudur (`fulltext_adjudication`), `fulltext_fetch` koşusu `completed` olunca kendiliğinden kuyruğa girer; getirme koşusu ve bütçesi aynı kalır; (2) koşu başına iş sınırı 20 / 50 / 150, iş başına iki çağrı (40 / 100 / 300), gerisi `not_read_yet` kalır ve sonraki koşu oradan sürer; (3) uygulayan Grok 4.7, inceleyen Opus — Grok bu depoda ilk kez uygular. Plan kararları (dosyada adıyla): çağrı başına tek iş ve 12 seçilmiş pasaj (8'e kadar ölçüt pasajı, parça parça turlarla, dilim 11'in `criterion_order`'ıyla; kısa metin bütün verilir); parça başına `present` / `absent` / `unclear` + birebir alıntı; alıntı yalnızca modele gösterilen sayfaların metninde, `exact` / `normalized` ile doğrulanır; `include` yalnızca iki koşu her parçada anlaşır ve **her** alıntı doğrulanırsa; iki `not_met` → `criterion_absent` → `excluded`; iki yeni gerekçe kodu (`fulltext_runs_agree_unresolved`, `pdf_identity_unconfirmed`); kimliği doğrulanmayan PDF çağrı yapılmadan kuyruğa gider (kullanıcı yüklemesi muaf); `abstract_promise_absent` kurulmaz; `work_outcome` eskimiş kod kararını artık atlar (dilim 10'un açık noktası). Dört ders kısıt olarak dosyada: ölçüt `frozen_criterion` ile okunur ve planda dondurulur, taze / eskimiş / yanıtsız / geçersiz durumların her biri adıyla yazıldı, kimlik okuma anında yeniden hesaplanır; yalnızca araştırmaya üye sürümler okunur; `store.step` yalnızca yürüten yolda; **yineleme payı eklenmedi** — onarım ve başarısız çağrı koşunun toplamından öder, bedeli sondaki işlerin `not_reached` kalmasıdır ve özet bunu sayar, iş yarım gönderilmez. **Ölçülmedi:** istem hiçbir modelle denenmedi; iş sınırı, 12 pasaj, 8 / 4 bölüşümü, alıntı alt sınırı, süre, etiket doğruluğu (dilim 13 duman, dilim 24). **Sahip görmeli:** kod ilk kez kullanıcı onayı olmadan `included` yazıyor; `criterion_absent` "gösterilen pasajlarda yok" demektir ve işi `excluded` yapar; kuyruğa giden beş nedenin ekranı yok (dilim 16–18). **Uygulandı (22 Eylül 2026), tek commit.** Migration `0046` (koşu türü `fulltext_adjudication`), karar `D85`. `skill_package_hash` önce `sha256:8f0e6cfb9116b5fba081d5959a45d04d9704b12ff38671f54b0400e540664d47`, sonra `sha256:b35b4f4960276c535452847693f76ea973f5e5a2417d51bf93527b9b82bb5e7c`. Yeni: `backend/deixis/workflow/adjudication.py`, migration `0046`, `contracts/research/fulltext-adjudication.schema.json`, `methods/deixis-research/references/fulltext-adjudication.md`, `tests/test_adjudication.py`, `tests/test_adjudication_flow.py` (birlikte 48 durum). Değişen: `flow._fulltext_adjudication` ve getirme `completed` olunca aynı turda kuyruk, `flow._topic_terms` (`_retrieve`'den çıkarıldı; seçim aynı), `decisions.work_outcome` (eskimiş kod kararı artık konuşmuyor), `rules` (`FULLTEXT_READ_LIMIT`, `LITERATURE_TASKS`; `CAPABILITIES` ve `TEST_EFFORT_BUDGETS` aynı), `reason_codes`, `config.Settings.fulltext_adjudication` (varsayılan `auto`), `store` aşama eşlemesi + `page_texts` + `STEP_OUTPUT_KINDS`, `protocol.thresholds.fulltext_adjudication`, `api/app.StartRun` (`legacy` 422), `skill.py`, `contracts.py`, `step-input.schema.json`, `determinism_stages`, `api.ts` + `labels.ts` + `i18n.ts` + `Transcript.tsx` + `BackgroundJobs.tsx` + `ResearchView.tsx`. **Onarım `usage["model_calls"]`'a yazılıyor:** her `start_model_session` +1, onarım denemesi ve başarısız çağrı dahil; depodan geri okunan yanıt yeniden yazılmıyor. **Sapmalar (adıyla):** (1) görev `LITERATURE_TASKS` içinde, `supported_tasks` değil; (2) kimliği doğrulanmayan iş `max_fulltext_reads` tüketmez; (3) `verify` ek `kind` döndürür; (4) parçası boş ölçüt tek parça `criterion` olarak gönderilir; (5) `whole_text` model alanı değil, saklanan adım girdisinden hesaplanır (gösterilen pasaj sayısı 12'den az ve sürümün sayfa sayısına eşit); (6) limiter 1 iken onarım sondaki işi okumadan bırakır; limiter 1'den büyükken sonraki iş onarım dönmeden çekilebilir; (7) başlamış bir işin ikinci çağrısını onarım yerse o işin bir koşusu kalır ve karar yazılmaz — sınamalar bu bütçe biçimini kullanmaz; (8) `derive_selection` durumu `pending` kalsa da origin'i `code_rule` yazar (mevcut davranış; kullanıcının seçimi `user` kalır); (9) bir işin beklenmeyen hatası koşuyu durdurmaz (D18), o işe karar yazılmaz; (10) planda olup model adımı açılmayan iş `not_reached` sayılır. **Ölçülmedi:** istem, iş sınırı, çağrı başına 12 pasaj ve 8 / 4 bölüşümü, alıntı alt sınırı, süre, etiket doğruluğu — canlı model çağrısı, kuru çalıştırma ve süre ölçümü yok (dilim 13, dilim 24). **Açık kalan:** `abstract_promise_absent`, kuyruk ekranı (dilim 16–18), SW11.9, üçüncü koşu, kod kapısı. Sınamalar: `PYTHONPATH=backend:. uv run pytest -q --tb=line` → 1590 geçti, 1 başarısız (bilinen `test_extraction_is_stopped_when_it_exceeds_the_memory_limit`; bu oturumda temel sayı kaydedilmedi). `npm run build` geçti; `npm run lint` çıkış 0, 17 önceden var olan uyarı, bu dilimin dosyalarında yeni uyarı yok. Playwright A–H 47 geçti. Kabul sunucusu `fulltext_adjudication="off"`. Adı istenen sınamalar: `test_an_unverified_quote_does_not_include`, `test_nothing_is_decided_while_the_model_is_off`, `test_a_user_exclusion_is_not_read_and_a_user_inclusion_is_not_overwritten`, `test_a_paused_run_whose_plan_is_exactly_the_limit_finishes_without_repeating_a_call`, `test_invalid_output_is_not_repeated_and_the_next_run_reads_the_work_with_two_calls`, `test_a_repair_leaves_the_last_work_not_reached_and_no_work_is_half_sent`, `test_a_completed_retrieval_run_is_followed_by_a_reading_run_that_includes_on_verified_quotes` (okuma adım açmaz: GET görünümü `run_steps` sayısını değiştirmez), `test_discovery_fetch_and_answer_runs_do_not_open_a_reading_step`. **İncelendi (22 Eylül 2026, Opus · high), düzeltme commit'i yok.** Diff dilim dosyasına karşı okundu ve sınamalar bağımsız koşuldu: `PYTHONPATH=backend:. uv run pytest -q` → 1590 geçti, 1 başarısız (aynı bilinen `test_extraction_is_stopped_when_it_exceeds_the_memory_limit`; bu dilimin dokunduğu hiçbir dosyayla ilgisi yok), `npm run build` + `npm run lint` çıkış 0. Yöntem dosyası dilim dosyasındaki metnin birebir aynısı ve `RUNTIME_FILES` / `SKILL.md` / `provenance.json` üçüne de bağlı; kural tablosunun sekiz satırı, yalnızca `exact` / `normalized` doğrulama, gösterilmeyen sayfanın aranmaması, `group_of` ile kullanıcı üstünlüğü ve eskimiş kod kararının susması hem kodda hem adıyla testte. **Doğruluk hatası bulunmadı**, bu yüzden düzeltme commit'i yok. **Görülüp bırakılan:** `_adjudication_plan` `read_plan`'i `limit` yerine `len(corpus)` ile çağırıp uygun **bütün** işlerin kimliğini denetliyor, yani sınırın dışında kalacak işler için de `_pdf_head_text` (o sürümün bütün pasajlarını okuyup birleştirme) olay döngüsünde koşuyor; `detailed` eforda ilk çağrıdan önce havuzun tamamı okunur. Hüküm doğru (kimlik sürüme bağlı, sonuç aynı), maliyet **ölçülmedi**. Sapma listesinin (7) ve (10) maddeleri incelemede de görüldü ve yazıldıkları gibi duruyor; `not_settled`'ın testi yok. |
| 13 | Uçtan uca duman testi | Duman | Fable · high | — (sonuç kaydı sahibe) | koşuldu (dört koşu; omurga sondan sona koştu, DeepSeek şemaya uymadı → 13a) | [dilim](sw-slice13-smoke-test.md) · [prompt](sw-slice13-prompt.md) | `fee0726`, `fe7c31d` (düzeltmeler; karar kaydı yok) · D86–D88 (izleyen dilimler) | Önkoşul 01–12 (kapandı). Kod yazmaz; tek araştırmayı gerçek modelle, ayrı veri dizini (`.local/sw-smoke-2026-09-22/data`) ve 8799 portunda, sorudan yanıta koşturur. **Plan varsayılanları (sahip sorulan üç seçeneğe cevap vermedi; başlamadan değiştirebilir):** `deepseek-flash` · high, kuantum sorusu (prisma-s belgesindeki tam metin), efor `quick` (≈ 50 çağrı). Beklenti koşudan önce `protocol.md`'de dondurulur (beş madde: keşif onaydan sonra biter, getirme ve okuma kendiliğinden açılıp biter, en az bir iş `code_rule` ile `included`, yanıt `structurally_valid` ve çapaları çözülür). Onay kartı `ask` kalır, öneri olduğu gibi API'den onaylanır. Bulunan hata sahte modelli testle düzeltilir, ayrı commit, koşu yeni dizinde baştan. Kalite yargısı yok; etiket doğruluğu, ifade ve yanıt kalitesi, ikinci konu, öbür eforlar ve gömme **ölçülmedi** (dilim 24). Koşuda bakılacak bilinen açıklar (dilim 12 ön kontrolü): limiter > 1'de tek koşuyla kalan iş; `close_ready`'de kayıtsız yutulan istisna. **Koşuldu 22 Eylül 2026, üç koşu, kanıt `.local/sw-smoke-2026-09-22/` (`result.md`, `protocol.md`, `log*.md`, `data` / `data-2` / `data-3`); omurga yanıta ulaşmadı, özet taramasının ilk model çağrısında `model_connection_not_ready` (`DeepSeek API unreachable: ConnectTimeout`) ile durdu ve beklentinin beş maddesinin beşi de tutmadı.** Birinci koşu `sw` yerine `legacy` akıştaydı: `serve --port` verilince `Settings` sıfırdan kuruluyor ve `search_workflow`, `protocol_approval`, `fulltext_fetch`, `fulltext_adjudication`, `query_strategy`, `model_concurrency` varsayılana düşüyordu; önce başarısız test (`tests/test_cli_options.py`), sonra `dataclasses.replace(settings, port=args.port)`, commit `fee0726`, tam pytest 1.592 geçti / 1 bilinen düşen (`test_extraction_is_stopped_when_it_exceeds_the_memory_limit`, bellek sınırından önce zaman aşımı). Düzeltme yeni davranış seçmediği için `D86` açılmadı; Playwright süiti gerçek başlatıcıyı değil `tests/acceptance/fixture_server.py`'yi başlattığı için hiçbir test bu yolu görmüyordu. İkinci ve üçüncü koşu (`data-2`, `data-3`) onay duraklamasına geldi, öneri API'den olduğu gibi onaylandı (`as_proposed` kullanılmadı), protokol donduruldu, altı sorgu sayfalandı ve kod aşaması kararları yazıldı; ikisi de özet taramasının ilk çağrısında aynı nedenle durdu. **Dış neden kapatılamadı:** duraklama anında kabuktan DeepSeek `models` uçnoktası gerçek anahtarla 0,34 sn'de HTTP 200 dönerken uygulamanın kendi `GET /api/connections/deepseek` isteği 10 sn'de yanıtlanamadı; `code:abstract_stage` olay döngüsünde 177 sn koştu ve model bağlaştırıcısı uygulama geneli `httpx.AsyncClient`'ı sağlayıcı istekleriyle paylaşıyor, sağlık denetiminin yeniden denemesi yok. Mekanizma **ölçülmedi**. **Kök neden sonradan bulundu ve düzeltildi (22 Eylül 2026):** `research_view` bu araştırma için olay döngüsünü 37 sn kilitliyordu (30 sn'si iş başına bir `work_versions` sorgusu, `source_versions.work_id` indekssiz); izleme betiğinin 10 sn'lik yoklaması döngüyü kilitleyince sağlık denetiminin 20 sn'lik bağlantı zaman aşımı doldu (deneyle doğrulandı: 25 sn kilitli döngü + 0,4 sn'lik bağlantı = `ConnectTimeout`). Düzeltme: migration `0047` (indeks), `Store.answer_versions` (görünüm için üç sorguda toplu), sağlık denetiminde bir yeniden deneme (DeepSeek, Gemini); aynı veritabanında görünüm 37 → 7,2 sn. Testler `tests/test_view_scaling.py` ve adaptör testleri; tam pytest 1.597 geçti. **4. koşu (`data-4`, `fe7c31d`, 08:00–08:28 UTC, `result-run4.md`):** duraklama noktası geçildi; keşif 16:56'da `completed` (10 çağrı, 6.356 aday), getirme 2:04'te `completed` (14 metin), okuma kendiliğinden açıldı, 1 iş `included`/`code_rule`; ama okuma 40 çağrının 34'ünü DeepSeek'in şemaya uymayan JSON'una harcayıp `budget_exhausted` ile duraklad, yanıt adımı da iki denemede `invalid_model_output` (`C1` etiketi, şema `c1` ister), yanıt `unverified_draft`. Beklenti: 1, 2, 4 tuttu; 3, 5 tutmadı. DeepSeek adaptörü şemayı API'ye vermiyor (`json_object`), Claude/Gemini/Codex veriyor; Luna/Gemini ile aynı adım **ölçülmedi**. Üçüncü koşunun sayıları: 7.377 kayıt / 6.891 iş, 71 sağlayıcı + 215 arama isteği, kod aşaması `blocks_in_title` 1.131, `both_blocks_missing` 1.354, `no_abstract` 2.086, `abstract_not_read` 2.142, `artifact_of_paper` 24, `notice_record` 14, `abstract_not_found` 202, `survey_title_word` 107; `selections` 1.355 `excluded`/`code_rule` + 3.138 `pending`/`code_rule`; 7 model çağrısı, 7/7 geçerli çıktı, 20.974/30.970 jeton, 28 dk 32 sn. **Ölçülmedi / ulaşılmadı:** özet taraması, getirme, okuma, `included` işler, yanıt ve inceleme adımları, dilim 12 ön kontrolünün iki açığı, ikinci konu, öbür eforlar, gömme açıkken davranış, etiket / ifade / yanıt kalitesi. Plandan sapmalar: keşif koşusu kendiliğinden başlamıyor (arayüz `startRun` çağırıyor, `Home.tsx:294`); yeni veri dizininde gömme örtük `gemini` geliyor ve açıkça `off` yapıldı; dondurulmuş `protocol.md` düzeltmeden önceki commit'i (`ea8b66b`) adlandırıyor. |
| 13a | Şemayı zorlayamayan modellerle çalışma (adaptör bayrağı, istemde şema + iskelet, kayıtlı eş ad düzeltmesi, onarım payıyla gönderim) | Kur (ölçülmedi) | Opus · high | tam (Fable · high) · yapıldı | kapandı (okuma yarısı tuttu; yanıt yarısı sahip kararı bekliyor) | [dilim](sw-slice13a-schema-lenient-models.md) · [prompt](sw-slice13a-prompt.md) | `a9aba33` + inceleme düzeltmesi · D86 | Önkoşul 13. Ana planda yok; dördüncü duman koşusundan çıktı (okuma 40 çağrının 34'ü, yanıt 2'de 2 geçersiz; DeepSeek adaptörü `output_schema`'yı hiç kullanmıyor, DeepSeek API'si `json_schema`'yı 400 ile reddediyor). Dilim 12 ön kontrolünün "limiter > 1 bütçe gediği" burada kapanır (Task 4). **Beşinci duman koşusu (DeepSeek, `data-5`, 22 Eylül 2026 10:08–10:46 UTC, `result-run5.md`):** dört koşu da `completed`; okuma 33 çağrının 32'si geçerli (dördüncü koşuda 40'ın 6'sı), hiç `budget_exhausted` yok, eş ad düzeltmesi hiç gerekmedi (`normalised` boş: iskelet yetti), 8 iş `included`/`code_rule`. Tek geçersiz okuma oturumu modelin `step_input_id`'yi bir harf büyük yazmasıydı (`envelope_mismatch`, doğru ret). **Yanıt yine `unverified_draft`:** ilk denemede `claims[].section` enum dışı serbest metin, onarımda o düzeldi ama başlık 17 kelime (`title_too_long`, sınır 15) ve onarım hakkı bitti. Yapısal gözlem: şema hataları ve anlam denetimleri ardışık raporlanıyordu, tek onarım iki tur hatayı kapatamıyordu; **düzeltildi (inceleme kuyruğu):** `validate_model_output` şema hatası olan taslakta da kural denetimlerini koşuyor ve şema hatasının işaret ettiği dalın dışındakileri aynı raporda veriyor (`_semantic_checks_best_effort`), iki fikstür beklentisi buna göre güncellendi. Beklenti 1–4 tuttu, 5 tutmadı. Süre 38 dk 36 sn (keşif 23 dk). `skill_package_hash` aynı kaldı (`sha256:b35b4f49…` önce ve sonra). Task 1–4 yapıldı, migration yok. Yeni: `tests/test_schema_lenient_models.py` (13 durum). Değişen: `adapter.py` (protokol `enforces_schema`), `deepseek.py` (şemayı sistem iletisine ekliyor), `gemini.py`, `claude.py`, `prompt.py` (`schema_appendix` + `_skeleton_node`), `contracts.py` (`OUTPUT_ALIASES`, `normalise_output`), `flow.py` (`_model_step` iskelet eki ve normalizasyon, `_abstract_stage` ve `_fulltext_adjudication` jeneratörlerinde onarım payı), `fakes.py` (`enforces_schema` parametresi), `test_deepseek_adapter.py` (2 yeni durum), `test_adjudication_flow.py` (3 testin bütçesi `repair_factor` yüzünden yükseltildi). Sapmalar: (1) onarım payıyla gönderim (`wanted = owed × repair_factor`) bütçesi tam uyuşmayan son işi göndermez: `budget=40` ve `repair_factor=2` ile 20 işin 19'u okunur (2 bütçe harcanmadan kalır); bu "önceden rezerve edilmez" kuralının kaçınılmaz sonucudur (koşu çalışırken onarım olup olmadığı bilinmiyor); (2) DeepSeek adaptörü `output_schema`'yı `json.dumps(indent=2)` ile ekliyor; biçim dilim dosyasında belirtilmemiş. Yapılmadı: beşinci duman koşusu (dilim 13'ün planıyla, Sonnet'e verilir). Ölçülmedi: DeepSeek'in iskelete uyma oranı, Gemini/Luna ile aynı adımlar, eş ad tablosunun başka modellerde gerekip gerekmediği, onarım payının okuma sayısına etkisi. |
| 13b | Crossref aramadan çıkar, DOI/künye doğrulamada kalır | Kur (ölçülmedi) | Opus · medium | toplu (13b–13d) | uygulandı, inceleme bekliyor | [dilim](sw-slice13b-crossref-verification-only.md) · [prompt](sw-slice13b-prompt.md) | D87 | **Uygulandı (22 Eylül 2026), Task 1–3.** Migration yok; `skill_package_hash` değişmedi. Süzgeç `Connector.searchable`: Crossref `False`, `available_providers()` yalnız aranabilirleri verir, yeni `verification_providers()` / `configured_providers()`. Araştırmanın kapsamı (`scope["providers"]`) **değişmedi** — `record_lookup:crossref` `in_scope` sınamasından geçmeye devam eder; modele sunulan `enabled_providers` ise süzülür. Derleyici (`compile_queries`, `compile_block_queries`) aranamayanı düşürür; saklı planı Crossref sorgusu taşıyan koşu o adımı `cancelled` / `provider_not_searchable` yazıp devam eder (adım durumu için migration gerekmesin diye `skipped` yerine mevcut `cancelled`). Arayüz: bağlantı kartında ve sayfasında rol etiketi (`Metadata verification` / `Record search`, iki dil). Yeni `tests/test_provider_roles.py` (9 sınama); `tests/test_provider_flow.py`'nin ikinci sağlayıcısı Crossref → bioRxiv, `tests/test_search_paging.py` Crossref'i ofset sayfalama temsilcisi olarak `searchable=True` ile kullanır. Testler: 1 başarısız (bilinen `test_documents.py` bellek sınırı), 1626 geçti. Önkoşul 13; 13a'dan bağımsız. Üçüncü koşu: Crossref 4.000 kayıt, 3.438'i yalnız onun bulduğu iş, 1.977'si özetsiz → Semantic Scholar özet araması 705 sn; kod aşamasından sonra 51 aday. Geri çağrım kaybı **ölçülmedi** (ölçüm adımında aynı konu Crossref'siz). Yedek arama kabul edilmedi. 13d ile paralel: 13b ana çalışma ağacında, 13d ayrı worktree'de (`DEIXIS-13d`); önce biten iter, öbürü `pull --ff-only` / (13d için) yalnızca kendi worktree'sinde `rebase origin/main` yapar. |
| 13c | Efora göre toplama sınırı (400 / 1.000 / 2.000 kayıt / sorgu) ve sağlayıcı bekleme kuralı (`quick` beklemez); hedef süreler 5 / 10 / 15 dk, ölçümle kalibre | Kur (ölçülmedi) | Opus · medium | toplu (13b–13d) | dosya hazır | [dilim](sw-slice13c-effort-collection-limits.md) · [prompt](sw-slice13c-prompt.md) | D88 | Önkoşul 13b. Zaman sınırı yok (yeniden üretilebilirlik). Üçüncü koşu: 28 dk'nın ~20'si sağlayıcılarda, 3'ü kod aşamasında, <3'ü modelde. Sabitler elle seçildi; ölçüm adımı (üç efor × bir konu) süreleri D88'in altına yazar, sabitler sonra ayarlanır. |
| 13d | Kod aşamasının hızı (`code:abstract_stage` 177 sn, `code:criterion` 89 sn, `research_view`'ın kalan 7 sn'si); çıktı bayt bayt aynı | Kur | Opus · medium | toplu (13b–13d) | yapıldı (inceleme bekliyor) | [dilim](sw-slice13d-code-stage-speed.md) · [prompt](sw-slice13d-prompt.md) | — (davranış değişmez) | Önkoşul 13; bağımsız. Migration yok (kalan maliyet indeks değil, `canonical_json` özeti). **Profil** (`data-3` kopyası, 7.377 aday / 7.769 kayıt / 4.695 iş; cProfile yükü süreleri şişirir): `_abstract_code_stage` **203,4 → 4,9 sn** (hedef ≤ 20 ✓); `research_view` **7,2 → 5,0 sn** (hedef ≤ 3 ✗); `code:criterion` yerel payı **0,001 sn** — 89,3 sn'nin 88,8'i üç model çağrısı (ağ), yerelde düzeltilecek bir şey yok, hedef ≤ 15 sn bu dilimde **karşılanamaz**. Önceki en sıcak üç çerçeve (tottime): `sqlite3.Connection.execute` 95.557 çağrı / 98,8 sn, `store.work_heads` 9.391 çağrı / 74,8 sn (kümülatif 173,3), `decisions.work_outcome` 4.695 çağrı / 11,7 sn. Düzeltme: `DecisionStore.facts` (bütün araştırma üç ifadede) + `derive_selections` (tek işlemde `executemany`), `store.work_ids`, `views.py`'de sürüm sıralaması sözlükle (görünümün kendi Python payı 2,19 → 0,15 sn). `research_view`'ın kalan 4,7 sn'si kaynak başına 8 sorgu (62.177 ifade) — dilim dosyası bunu adlandırmıyor, ayrı bir toplu-okuma dilimi gerekir. Kanıt: aynı kopyada karar/seçim/geçmiş satırları ve adım çıktısı **birebir aynı** (4.876 karar, 7.769 seçim, 16.957 geçmiş); testler `test_abstract_flow.py::test_the_stage_writes_the_same_rows_when_its_reads_are_batched` ve `test_code_stage_scaling.py::test_the_stage_s_whole_research_reads_do_not_grow_with_the_number_of_records`. 13b ile paralel worktree'de koştu. |
| 14 | Kaynak yönlendirme | Kur (ölçülmedi) | Opus · medium | toplu | plan | | | |
| 15 | Atıf zinciri | Kur | Opus · high | toplu | plan | | | |
| 16 | İnsan kuyruğu (arka uç) | Kur | Opus · high | tam | plan | | | |
| 17 | İnsan kuyruğu (arayüz) | Kur | Opus · medium | toplu | plan | | | |
| 18 | PDF bekleyenler ve kullanıcının eklediği PDF | Kur | Opus · medium | toplu | plan | | | |
| 19 | Prob seti, kol ve sinyal tabloları, durma kuralı | Kur | Opus · high | toplu | plan | | | |
| 20 | Akış sayıları, denetim örneği, ezme sayısı, PRISMA-S dışa aktarımı | Kur | Opus · medium | toplu | plan | | | Plan notu (21 Eylül 2026): ana ekrandaki derinlik açıklaması (`apps/web/src/Home.tsx` `effortOptions`: "20 / 250 / 300 aday", "10 / 25 sonuç") `legacy` sınırlarını anlatıyor; `sw` araştırmasında dilim 09'dan sonra yanlış olur (sorgu başına 2.000 kayıt okunur, model 40 / 100 / 300 özet okur). Bu dilim metni akışa göre doğru söyletir; en geç dilim 24'ten (varsayılan değişmeden) önce kapanmalı. |
| 21 | Yerleşik yerel gömme | Kur | Opus · medium | toplu | plan | | | |
| 22 | arXiv LaTeX kaynağından okuma | Kur (ölçülmedi) | Opus · high | toplu | plan | | | |
| 23 | Kullanıcının onayladığı kod kapısı | Kur (isteğe bağlı) | Opus · high | tam | plan | | | |
| 24 | Varsayılanı değiştir ve kapat | | plan | Fable · high | — | | | |

## Yapılan incelemeler

Her inceleme bir satır: ne bulundu, nasıl kapandı. Bulgu yoksa "bulgu yok" yazılır. Kayıt, hangi tür dilimde incelemenin işe yaradığını gösterir; model ve efor seçimi buna göre gözden geçirilir. "İnceleyen" sütununa gerçekten kullanılan model ve efor yazılır, planlanan değil.

| Tarih | Dilim | Tür | İnceleyen | Bulgu | Düzeltme |
|---|---|---|---|---|---|
| 2026-09-20 | 01 | tam | Fable (plan sohbeti; efor kaydedilmedi) | Aynı kapsam revizyonunda ikinci keşif koşusu, model başka bir plan verince `internal_error` ile çöküyordu (değişen protokol gerekçe istiyor, akış vermiyordu); görünüm eski koşuya yeni protokolün özetini gösteriyordu. Kaynak: dilim dosyasında eksik durum, kod hatası değil. Ayrıca plandaki test komutu iki testi toplamıyordu (`PYTHONPATH=backend` yerine `backend:.`). Uygulayanın yerinde sapması: protokol kaydının silme tetikleyicisine kalıcı silme istisnası. | `d72f205` |
| 2026-09-20 | 02 | tam | Fable · medium | `undo_human` geri getirdiği kararı güncel kapsam revizyonu ve protokol özetiyle damgalıyordu, böylece eskimiş bir karar taze görünüyordu; `work_outcome`, kazanan sonucu birden çok sürüm taşıdığında ilk karar verileni adlandırıyordu (sıraya bağlı), artık işin başını, yoksa en küçük kimliği adlandırıyor. | `3d4a6c9` |
| 2026-09-20 | 03 | tam | Fable · high (plan sohbeti) | Korumanın reddettiği birleştirme (`work_already_has_published`) kayıt her yeniden bulunduğunda açık satırı `superseded` kapatıp aynısını yeniden yazıyordu: saklanacak hüküm açık satırla karşılaştırılmadan önce belirlenmiyordu. Tekrarlanan çağrı durumu; dilim dosyası soruyu soruyordu ama koruma yolu için test yoktu. Karar tablosu dosyaya uygun; `legacy` yolu değişmemiş. 127 çiftte yanlış birleşme yok. | `237801f` |
| 2026-09-21 | 04a | tek başına (tabloda toplu; sahip bitince istedi) | Fable · high (plan sohbeti) | İki bulgu. (1) `vocabulary_empty` ya da `vocabulary_too_broad` ile duran koşu sürdürülünce saklı çıktı yolu duraklatma denetimini atlıyor, koşu sorgusuz `completed` oluyor ya da reddedilen geniş sorguyu arıyordu; tekrarlanan çağrı durumu, testi yoktu. (2) Dil kuralı (dilim dosyasının kendi hatası, uygulayanın değil): işlev sözcüğü payı 0,2 eşiği, 11 sözcükte 2 işlev sözcüğü olan düz bir İngilizce soruyu ve her anahtar sözcük listesini `other` sayıp `key_terms_needed` ile durduruyordu. Sözcük listelerinde alan terimi yok; `legacy` yolu aynı. | `8ef025b` |
| 2026-09-21 | 04d | tam | Fable · high (plan sohbeti) | Bir bulgu. Model açık ama işe yaramazken (üç geçerli koşu hiçbir ifadede anlaşamıyor, ya da çoğunlukla her ifadeyi `claim` / `not_a_term` sayıyor) bütün ifadeler sorgudan düşüyor, geçit boşalıyor ve koşu `vocabulary_empty` ile duruyordu; kural tek başına aranabilir bir sorgu veriyordu. SW17.5 "ilk arama model kapalıyken de koşar" der; dilim dosyası modelin *açık ve yanlış* olduğu durumu yazmamıştı, uygulayan tek koşuluk halini kendi yakalayıp düzeltmişti. Kaynak: dilim dosyasında eksik durum. İzin listesi, onarımsız tek deneme, `legacy` ve `key_terms` yolları, sürdürmede saklı çıktı ve protokoldeki `block_origin` dosyaya uygun; `max_schema_repairs` öbür görevlerde aynı değeri taşıyor. Testler 932 geçti, bilinen 1 başarısızlık. | `249774d` |
| 2026-09-21 | 04c | tek başına (tabloda toplu; sahip bitince istedi) | Fable · high (plan sohbeti) | İki bulgu, ikisi de dilim dosyasının (planın) eksiği. (1) Türetilmiş istek izni sayfa başına tek istek sayıyordu, ama hız sınırı ve ağ yeniden denemeleri de istek sayılıyor ve koşu genelinde yalnızca 4 yedek vardı: her sayfası bir 429'dan sonra başarılı olan iki sağlayıcılı okuma `budget_exhausted` ile durdu, sürdürme yine durduruyordu; tarama hiç başlamıyordu. Tekrarlanan çağrı / bütçeyle etkileşim durumu, testi yoktu. (2) Kayıt getirmeyen ama imleç taşıyan sayfa okuma toplamını büyütmediği için sorgu, izin bitene kadar boş sayfa isterdi. `legacy` yolu, sürdürmede sayfa tekrarı, başarısız sayfa kuralı, `first_rank`, `unread` görünümü ve protokol dosyaya uygun. Testler 1.000 geçti, bilinen 1 başarısızlık. | `78ae7e5` |
| 2026-09-21 | 04b | tek başına (tabloda toplu; sahip bitince istedi) | Fable · high (plan sohbeti) | İki bulgu, ilki dilim dosyasının eksiği. (1) Tekrarlanan çağrı: aday havuzu kapsam revizyonunun bütün adaylarıydı, genişleme kolunun kendi getirdikleri dâhil; aynı kapsamda ikinci keşif koşusu onlardan da öğreniyor, her koşu aramayı sorudan biraz daha uzağa taşıyordu. (2) `count_yields` olay döngüsü iş parçacığında 3.000 aday × 18 terim için 1,1 sn sürüyordu (ölçüldü); alt dizi aramasına çevrildi. Dosyaya uygun: model çağrısı yok, iddia sözcüğü hiçbir sorguda yok, ilk protokol kaydı değişmiyor ve ikinci revizyon yalnızca terim kabul edilince yazılıyor, sürdürmede sayım ve sayfa tekrarı yok, istek izni iki turu kapsıyor, `legacy` yolu aynı. Canlı kuru çalıştırma okundu: 20 adayın hepsi alanın gerçek terimi, 12'si kabul. Testler 1.040 geçti, bilinen 1 başarısızlık. | `9faac65` |
| 2026-09-21 | 05 | tam | Fable · high (plan sohbeti) | Üç bulgu, ilki dilim dosyasının eksiği. (1) Kapsam revizyonuyla etkileşim: dosya "özeti sonradan gelen" kaydın kararını düzeltiyordu ama "artık derleme sayılmayan" kaydınkini yazmamıştı; soru revize edilip güçlü sözcük sorunun konusu olunca eski `survey_title_word` satırı güncel kalıyor, tarama süzgeci onu okuyor ve kayıt yeni soruda sessizce taranmıyordu. (2) `external_links`, doldurulan özetleri yük dosyası adıyla buluyordu; aramalar da yükünü adım kimliğiyle sakladığı için koşunun özetli bütün kayıtları ikinci kez bağlantı kuralından geçiyordu (sonuç aynı, iş boşa; testi yoktu). (3) `held_from_screening` her iş başı için sürümleri ve kararlarını ayrı sorguyla okuyordu: 2.000 SYNTHETIC adayda 1,09 sn, olay döngüsü iş parçacığında ve iki kez (ölçüldü; tek sorguyla 0,24 sn, `record_flags` adımı 1,4 → 0,5 sn). Dosyaya uygun: `legacy` isteği ve gövdesi aynı, `derive_selection` çağrılmıyor ve hiçbir seçim `code_rule` değil, özetsiz kayıt modele gitmiyor, ikinci koşu karar / işaret / bağlantı satırı eklemiyor, parça ortasında sürdürme DOI tekrarlamıyor, sorgu istekleri kendi sayacında, `save_link` koruması ve `undone` belleği dış bağlantıda da geçerli, migration satırları koruyarak yeniden kuruyor. Uygulayanın bildirdiği sapma (12b iki mevcut test beklentisinde 03'ün korumasından önce konuşuyor; hüküm aynı) kabul edildi. S2'nin yinelenen kimliğe yanıtı bir canlı istekle doğrulandı. Testler 1.129 geçti, bilinen 1 başarısızlık. | `ea7741f` |
| 2026-09-21 | 06 | tek başına, dar (tabloda toplu; sahip bitince istedi) | Fable · high (plan sohbeti) | Bir bulgu. Uygulayan, ölçüt adımının üç çağrısına yer açmak için üç efor ön ayarının `max_model_calls` değerini yükseltmişti; ön ayarlar `legacy` araştırmalarda ve yanıt koşularında da kullanıldığı için onların bütçesi ve yeni `legacy` protokol gövdelerindeki `budget` alanı da değişiyordu ("legacy aynı kalır" kuralının ihlali; mevcut testler bütçeyi elle verdiği için görmedi). Üç çağrı artık yalnızca `sw` keşif koşusuna `api/app.py`'de ekleniyor, iki yöne de test yazıldı. Okunan: `workflow/criterion.py`, `flow._criterion`, `store.frozen_criterion`, `protocol.py`, `rules.py`; dosyaya uygun: uzlaşı kuralı ve taban koşu, `criterion_origin`'in `CRITERION_FIELDS` dışında durması, dondurulmuş ölçütün kapsam revizyonları arasında bayt bayt geri alınması, genişleme revizyonunun ölçütü taşıması, yöntem dosyasında deneme sorularının geçmemesi. **Okunmayan:** sözleşme denetimi (`_check_criterion_proposal`), fikstürler ve testlerin içi; toplu incelemede (07–08 ile) bakılmalı. Görülüp bırakılan: dilim dosyası iki koşu başarısız olunca üçüncü çağrının atılmamasını istiyordu, uygulayan atıyor ve bunu testle sabitlemiş (04d ile aynı davranış; asılı modelde bir zaman aşımı daha); sapma satır 06'nın sapma listesinde değil yalnızca commit iletisinde yazıyordu. Testler 1.180 geçti, bilinen 1 başarısızlık. | `9b8dfce` |
| 2026-09-21 | 07 | tek başına (tabloda toplu; sahip bitince istedi) | Fable · high (plan sohbeti) | Bir bulgu, dilim dosyasının (planın) hatası. Tarama listesi `(öneri aldı mı, sıradaki yer)` ile diziliyordu; iki parti arasında duraklatılıp sürdürülen koşuda ilk partinin kayıtları sona gidiyor, `screening:N` anahtarları başlangıç yerine bağlı olduğu için sıranın 41–80. yerleri saklı ilk partinin yuvasına düşüp hiç taranmıyor, yerlerine kesmenin dışındakiler taranıyordu (tekrarlanan çağrı durumu; uygulayanın sürdürme testi koşuyu ilk partide durdurduğu için görmedi). Artık yalnızca **önceki** koşuların taradığı kayıt sona gidiyor. Aynı hata `legacy`'de de var ve eski: ölçüldü, 120 adayın 40'ı taranmadan koşu `completed` oluyor; `legacy`'ye dokunulmadı. Okunan: `ranking.py` tamamı, `flow`, `store`, `decisions`, `protocol` değişiklikleri; dosyaya uygun: sinyaller `fuse.py`'ye sadık, eksik sinyal sonda ve toplamda, eşitlikte kimlik, kayıt başına sorgu yok, `legacy` isteği ve gövdesi aynı, genişleme revizyonu `signals`'ı taşıyor, hiçbir karar tablosuna yazılmıyor. **Okunmayan:** `openalex.py` değişikliği, migration dosyası, saf işlev testlerinin ve tekrar aşamasının içi; toplu incelemede (08 ile) bakılmalı. Testler 1.231 geçti, bilinen 1 başarısızlık. | `e2d9e7e` |
| 2026-09-21 | 08a | iki inceleme: önce Opus · high (ayrı sohbet), sonra Fable · high (plan sohbeti), aynı commit | Opus · high, sonra Fable · high | **Opus:** bloke edici bulgu yok; dosyaya uygunluğu, kabul koşulunun testini ve genişleme revizyonu tuzağını doğruladı, üç küçük not (satır 08a'da). **Fable:** iki bulgu, ikisi de dilim dosyasının (planın) eksiği. (1) Önceki onay ölçütü de kapsıyor sayılıyordu; ilk koşuda model kapalıyken ölçütsüz onay verilince sonraki koşunun yeni model ölçütü kullanıcı görmeden donuyordu (SW15.3; tekrarlanan çağrı durumu). (2) 08a ile 08b çelişiyordu: 08b kartı fazla geniş öneriyi düzelttiriyor, ama akış o öneriyi onaya ulaşmadan `_vocabulary`'de durduruyordu. İkisi de önce başarısız testle gösterildi. **Yöntem notu:** Opus dosyaya uygunluğu eksiksiz denetledi, ama dosyanın yazmadığı durumları aramadı; iki bulgu da o türdendi. Tek karşılaştırma, genelleme değil. Testler 1.299 geçti, bilinen 1 başarısızlık. | `fd89a88` |
| 2026-09-21 | 08b | tek başına (sahip bitince istedi) | Fable · high (plan sohbeti) | Üç bulgu, hepsi "kaydedilmiş durumu göster, yapının yapmadığını söyleme" türünden (AGENTS.md). (1) `sw` koşusunun plan adımları (`code:vocabulary`, etiketleme, ölçüt, onay) zaman çizelgesinde hiçbir faza bağlı değildi; onay bekleyen koşu "Search plan · Waiting" gösteriyordu. 04a'dan beri böyleydi, onay duraklamasıyla görünür oldu. (2) İki kart metni bugün var olmayan davranışı anlatıyordu (dışlama sözcükleri kaydı dışarıda tutar). (3) Kartın "sıralar" dediği kullanıcı eklemesi sonuç terimi sıralamaya girmiyordu (`ranking.query_vocabulary`). Bakılan: kartın düzenlenebilirlik mantığı (`submitted` + `paused` yeniden açılıyor, doğru), düz Sürdür'ün gizlenmesi, açık ve koyu tema ile 390 px ekran görüntüleri (taşma, kesilme yok). **Okunmayan:** `ProtocolApproval.tsx`'in taslak → düzeltme paketi dönüşümü satır satır, `i18n.ts` Türkçe metinlerinin tamamı, senaryo H'nin içi. Build, lint temiz; Playwright 42 geçti (A–G + H); pytest'in sıralama, akış ve belirlenimcilik dosyaları geçti, **tam pytest bu incelemede koşulmadı** (arka uçta tek değişiklik sıralamanın sözcük kümesi). | `de72ee0` |
| 2026-09-21 | 09 (+ Task 5) | tam | Fable · high (plan sohbeti) | İki bulgu. (1) `ae32b2d`'nın getirdiği hata, tekrarlanan çağrı / bütçeyle etkileşim türünden: `_abstract_stage` gönderdiği her (parti, koşu) işini bütçeye yazıyordu, saklı adımı geri okunan iş de dâhil; dört çağrıdan sonra duraklatılıp sürdürülen ve bütçesi altı çağrının tamamına yeten koşu dört çağrıda kalıyor, ikinci partinin ödenmiş yanıtlarını atıyor ve `completed` oluyordu (önce başarısız testle gösterildi). Uygulayanın sürdürme testi bütçeyi geniş bıraktığı için görmedi. (2) Dilim dosyasının (planın) eksiği: tek geçit bloklu dağarcıkta `both_blocks_missing` tek bloğun yokluğuyla kapatıyor, seçim `excluded` + `code_rule` oluyordu. Okunan: `abstract_stage.py` tamamı, `flow`'un özet aşaması ve ortak gönderim döngüsü, `contracts` / `decisions` / `ranking` / `protocol` / `api/app.py` değişiklikleri, yöntem dosyası, değişen mevcut testlerin diff'i (beklenti değişiklikleri dilimin sonucu: model kapalıyken koşu artık kod aşamasıyla bitebiliyor). Dosyaya uygun: özetten `included` çıkmıyor, plan donduruluyor ve sürdürmede yeniden hesaplanmıyor, durdurulan koşuda uçuştaki çağrılar adımını yazıyor ve parti kapanmıyor, insan kararı atlanıyor, dilim 05'in kodlarına dokunulmuyor, ön ayar tablosu aynı, `legacy` dalı yerinde. **Okunmayan:** fikstür dosyaları, tekrar aşamasının içi, `test_abstract_flow.py`'nin gövdesi, arayüzün iki satırı. Testler 1.432 geçti (bilinen 1 başarısızlık ayrı tutuldu). | `a029108` |
| 2026-09-21 | 08c | tek başına (tabloda toplu; sahip istedi) | Fable · high (plan sohbeti) | Üç bulgu ve bir kayıt düzeltmesi. (1) Bütçeyle etkileşim, dilim dosyasının (planın) hatası: dosya, başarısız isteğin yinelenmesinin `budget_exhausted` ile düşeceğini varsayıyordu; oysa bütçe koşunun toplamına bakar ve onay anında taramanın payı henüz harcanmamıştır. Başlatılan her çağrı (başarısız olan da) koşuya yazıldığı için her ücretli yineleme özet taramasından bir çağrı alıyor, iki yineleme sıranın sonundaki 20 kaydı sessizce `abstract_not_read` bırakıyordu. (2) Boş yanıt: model hiç ad önermeyince (geçerli çıktı) görünüm `none` ve düğme açık diyor, rota ise `already_suggested` ile 409 dönüyordu; kartın "Model yeni bir ad önermedi" satırı hiç görünmüyordu ve boş yanıt sonraki karta taşınmadığı için model aynı soru için yeniden sorulabiliyordu (SW2.6). Testi yoktu. (3) Tekrarlanan çağrı / etiketlemeyle etkileşim, planın eksiği: taşınan öneriler önceki koşunun dağarcığına göre süzülmüş hâliyle kopyalanıyordu; sonraki koşunun etiketlemesi başka ifadeler kurabildiği için (bunu `approval.applicable` zaten söylüyor) kart, artık öneride duran bir ifade için **Ekle** sunabiliyor (gönderim bütünüyle 422), iddia ifadesi taşıyan bir ad eklenebilir görünüyor ya da çapası artık aranmayan bir ad yanlış blokla duruyordu. Kayıt düzeltmesi: D82 madde 8 ve satır 08c "öneri istemeyen koşunun özeti bu dilimden önceki değerdedir" diyordu; gövde koşunun bütçesini taşır ve `max_model_calls` her `sw` keşif koşusunda `SUGGESTION_CALLS` kadar yüksektir, yani alan eklenmez ama özet değişir. Dosyaya uygunluk, "eklenmeyen öneri sorguya girmez", köken türetmesi, sürdürme ve 409'lar doğrulandı; yöntem dosyası metni değişmemiş. **Ölçülmeyen aynen duruyor:** istem hiçbir modelle denenmedi. Not: inceleme sırasında dilim 10'un uygulaması aynı çalışma ağacında sürüyordu; inceleme commit'i yalnızca kendi parçalarını taşır. Tam pytest yalnızca sahnelenen ağacın dışa aktarımında koştu (1437 geçti, bilinen tek başarısızlık hariç); `npm run build`, lint ve Playwright (47 geçti) ortak ağaçta, o yarım işle birlikte koştu. | `eee89f3`. Ücretli çağrı sayısı `SUGGESTION_CALLS`'a ulaşınca rota 409 döner, görünüm `suggestion_call_spent` der, kart **Yeniden dene** yerine nedeni gösterir; çağrı başlamadan düşen istek (bağlantı hazır değil) yinelenebilir. Boş yanıt `ready` sayılır ve taşınır. `suggestions.carry` taşınan satırları yeni öneriye göre yeniden süzer, sayımı korur, çapası kalmayanı `anchor_not_searched` ile listede tutar (köken türetmesi bozulmasın). Beş yeni test; bir test yeniden yazıldı (ücretsiz başarısızlıktan sonra yineleme), birinin beklentisi bulguya göre değişti; Playwright H'nin hata durumu güncellendi. `skill_package_hash` aynı. |
| 2026-09-21 | 10 | tek başına (tabloda toplu, 12'den önce; sahip bitince istedi) | Fable · high (plan sohbeti) | Dört bulgu; ilk ikisi dilim dosyasının (planın) hatası. (1) Tekrarlanan çağrı: DOI araması satırları (`pdf_discovery_runs`) koşunun değil araştırmanın geçmişidir. Dosya aramayı "henüz yapılmadıysa" koşuluna bağlamıştı ve yanıtsız yolları bütün geçmişten sayıyordu; bir sağlayıcısı 429 / zaman aşımı veren iş sonraki koşuda **yeniden aranmıyor**, eski satır yüzünden de hiç karar alamıyordu: her koşu onu plana alıyor, hiçbiri kapatamıyordu ("sonraki koşuda yeniden denenir" yalnızca kaydın kendi bağlantısı için doğruydu; testi de yalnızca onu sınıyordu). (2) Kaydın kendi bağlantısı 403 / 404 verince arama `_acquire_pdf` içinde dar hâliyle (`other_versions` olmadan) koşuyor, sonra "zaten arandı" diye sürüm satırı yolu hiç denenmiyordu — sahibin 3. kararı en çok gereken kayıtlarda uygulanmıyordu (uygulayan bunu 4. sapma diye adıyla yazmıştı). (3) Kayıtlar araştırmalar arasında ortaktır: başka bir araştırmanın açtığı sürüm satırı bulunuyor ama bu araştırmaya üye yapılmıyordu; metin kütüphanede dururken iş `no_fulltext` alıyor ve bir daha denenmiyordu. (4) `_other_copy_found` adımı `store.step` ile okuyordu; o işlev yoksa açar, böylece kendi bağlantısından okunan her işin zaman çizelgesinde hiç koşmayacak `pending` bir `pdf_other_copy` adımı kalıyordu (AGENTS.md: kaydedilmiş durumu göster). Doğrulananlar: plan dondurma ve Ders 1 (sınır planda, canlı sayaç yok), `legacy` / `answer` / `pdf_collection` varsayılanları, `MAX_DOWNLOADS_PER_RUN`'ın bu koşuya uygulanmadığı, kimlik denetimi, migration, kuyruğa girme ile koşu tamamlama arasında `await` olmadığı. **Dokunulmayan, adıyla:** duraklatma isteği son denetimden sonra gelirse keşif koşusu tamamlanır ve getirme yine kuyruğa girer (zamanlamaya bağlı, testi yazılamadı); yanıtsız arama yinelenince reddetmiş aday bağlantılar da yeniden istenir (`acquire_for_source`'un eski davranışı). Ölçülmeyenler aynen duruyor. | `8dca91a`. Yalnızca her sağlayıcının en yeni arama satırı yanıtsız sayılır ve yanıtsız araması / kopyası olan iş yeniden aranır (`_unanswered_lookups`); `_acquire_pdf(other_versions=)` getirme koşusunda `True`, yol `lookup_version` olarak kaydedilir; `open_lookup_version` var olan satırı soran araştırmaya üye yapar, dosya yeniden istenmez; arama adımı açılmadan okunur (`_other_copy_step`). Dört yeni test, mevcut beklenti değişmedi. `skill_package_hash` aynı, migration yok. |
| 2026-09-22 | 11 | tek başına (tabloda toplu; sahip dış değerlendirmeyi getirdi) | Grok 4.7 (değerlendirme) + Fable · high (doğrulama ve düzeltme, plan sohbeti) | Bir bulgu, üç test boşluğu. (1) Kabul koşulu tutmuyordu: `_retrieve`'de kaynak başına sayaç, kalabalık dal (D55) bir kaynağa özet + 2 sayfa vermiş olsa da 1'den başlıyor; kısa kaynaklar kalabalık seçimi dolduramadığında (17 kaynak, 16'sı tek sayfalı: 35 / 48) ölçüt turları ve konu sırası uzun kaynağa 5 pasaj daha ekliyor, toplam 8 (`MAX_PASSAGES_PER_SOURCE` 6). Kusur dilimden eski ve `legacy` dalında da var; eşiksiz ifade listesi onu `sw`'de kolay ulaşılır kılıyor. Mevcut sınır testi geniş durumda, kalabalık testler limiti tam dolduran durumda koşuyordu; iki dalın aynı çağrıda birleştiği durumun testi yoktu. (2) `fulltext_fetch` koşusunun `criterion_phrases` adımını açmadığı sınanmıyordu (dilim dosyası adıyla istiyordu). (3) Yönlendirme değişince eski ifadelerin düşmesi sınanmıyordu. Görülüp bırakılan: başka araştırmanın PDF'i testi `_retrieve`'e süzülmüş liste veriyor, sızıntıyı değil listenin dışına çıkılmadığını gösteriyor; ürün yolu `included_works` + `answer_version` ile doğru. Doğrulananlar (değerlendirmeden, kod okunarak): tek commit, desen ve puan, `frozen_criterion` tek okuma yolu, adımın dondurulması, `legacy` gövdesi ve seçimi, `skill_package_hash` aynı. **Ölçülmeyenler aynen duruyor;** kusurun üründe ne sıklıkta oluştuğu da ölçülmedi. | `8abdef3`. `sw` dalında sayaç seçilmiş pasajların gerçek sayısından başlar; `legacy` dalı bayt bayt aynı, kusuru D84 Limits'te. Üç sınama: yeni `test_a_crowded_selection_that_leaves_room_still_holds_every_source_to_the_passage_cap` (düzeltmeden önce 8 == 6 ile düştü) ve `test_phrases_frozen_under_another_steering_are_left_behind`; `test_no_other_run_kind_opens_the_step_…` artık `fulltext_fetch` koşusunu da kapsıyor. Mevcut beklenti değişmedi. `skill_package_hash` aynı, migration yok. |
| 2026-09-22 | 12 | tam | Opus · high (inceleme sohbeti); bu satırı inceleme commit'inin iletisinden plan sohbeti yazdı (inceleme kayda satır bırakmamıştı) | Fark dilim dosyasına karşı okundu; tam pytest, web build ve lint yeniden koşuldu; doğruluk hatası bulunmadı. Görülüp bırakılan: plan adımının kimlik denetimi sınırın dışında kalan işler için de koşuyor. Plan sohbetinin ön kontrolünde işaretlenen ve incelemede ele alınmayan iki nokta açık: limiter 1'den büyükken bir iş bütçe sınırında tek koşuyla kalabilir (D85 Limits'te adıyla var, testi yok); `close_ready` beklenmeyen istisnayı kayıtsız yutuyor. İkisi de dilim 13'te gözlenecek. Ölçülmeyenler aynen duruyor. | `118db10`, yalnızca satır 12 (`kapandı`). Kod değişikliği yok. |
| 2026-09-22 | 13 (duman, dört koşu) | duman (inceleme değil, koşu kaydı) | Fable · high (koşu 1–3, plan sohbeti), Sonnet (koşu 4, arka plan ajanı) | Üç bulgu. (1) `serve --port` bütün ayarları sıfırlıyordu; 8765 dışındaki her sunucu `legacy` akıştaydı (hiçbir test gerçek başlatıcıyı görmüyordu). (2) `research_view` 7.769 kaynaklı araştırmada olay döngüsünü 37 sn kilitliyordu (iş başına `work_versions`, `source_versions.work_id` indekssiz); izleme yoklaması döngüyü tutunca DeepSeek sağlık denetiminin 20 sn'lik bağlantı zaman aşımı doldu ve koşu `model_connection_not_ready` ile durdu; deneyle doğrulandı (25 sn kilitli döngü + 0,4 sn'lik bağlantı = `ConnectTimeout`). Görünüm 37 → 7,2 sn, sağlık denetimi bir kez yeniden deniyor. (3) Düzeltmeden sonra omurga sondan sona koştu; DeepSeek okuma adımında 40 çağrının 34'ünde, yanıt adımında 2'de 2 şemaya uymayan JSON verdi (adaptör şemayı hiç göstermiyor, API zorlamıyor); okuma `budget_exhausted` ile duraklad (dilim 12'nin bilinen bütçe gediği tetiklendi), yanıt `unverified_draft`. Tasarım kararı D86; dilimler 13a–13d açıldı. | `fee0726`, `fe7c31d`; kayıt `aa5e12d` |
| 2026-09-22 | 13a | tam | Fable · high (plan sohbeti) | Üç bulgu, ilki dilim dosyasının (planın) hatası. (1) Bütçe: dosyanın "onarım payıyla gönderim" (`owed × (1 + onarım)`) kuralı, onarım hiç olmasa da okuma koşusunun 20 iş yerine 19 okumasına yol açıyordu (D85: 2N çağrı N iş okur); uygulayan bunu bildirmiş ve üç mevcut testin bütçesini yükselterek geçirmişti, Task 4'ün "onarımsız koşuda 20'si okunur" testi ise gerçek akış yerine formül üzerinde yazılmıştı. Düzeltme: göndericiler eski hesabına döndü; `_model_step` `sw` aşamalarında (`budget_short="skip"`) sığmayan onarımı atlar (adım `invalid_model_output`, koşu devam eder) ve eşzamanlı göndericinin taşırdığı ilk çağrıyı `budget_exhausted` adımıyla kapatıp koşuyu duraklatmaz; tablo doldurma ve yanıt koşuları eskisi gibi duraklar (`test_table_extraction` bunu ilk denemede yakaladı). Yeni test `test_a_repair_the_budget_no_longer_holds_is_skipped_and_the_run_completes`; taşırma durumu (a) kodda var, deterministik testi yok. (2) `answer_review` için eş ad satırı hiç çalışmıyordu: normalizasyon yalnızca sözlük girdiye uygulanıyordu (inceleme çıktısı metin olarak gelir) ve satır var olmayan `claims` dizisini adlıyordu (`reviews[].claim_label`). (3) DeepSeek adaptörü şemayı ikinci kez sistem iletisine ekliyordu; saklanan StepInput bu kopyayı taşımadığı için kaldırıldı, model şemayı adımın sakladığı ekten görür (dilim dosyasının Task 1'i ile Task 2'si çelişiyordu). `skill_package_hash` aynı (`sha256:b35b4f49…`). Tam pytest 1.615 geçti. | inceleme commit'i |

## Sahip kararları

Öneriler ana planın §5'indedir.

| Karar | Konu | Durum |
|---|---|---|
| K1 | Eski ve yeni akışın birlikte yaşaması (araştırma başına bayrak) | kabul edildi, önerildiği gibi (2026-09-20); bayrak D70 ile geldi |
| K2 | Yeni aşama kararlarının `selections` ile ilişkisi | kabul edildi, önerildiği gibi (2026-09-20); dilim 02 uygular |
| K3 | Özet aşamasında model çağrı bütçesi | kabul edildi, değiştirilerek (2026-09-21): N = 40 / 100 / 300 iş (öneri 60 / 150 / 300 idi; sahip "hızlı olmalı, 100 yeter" dedi), çağrı başına 20 kayıt, iki koşu, eşzamanlı gönderim; ölçüm `.local/sw-abstract-batch-2026-09-21/`; dilim 09 uygular |
| K4 | Ölçüm modeli (`deepseek-flash`, efor `high`) | karar (22 Eylül 2026): DeepSeek kalır; ölçüm modeli değiştirilmez, uygulama kullanıcının seçtiği modelle çalışır (dilim 13a, D86). 13a'nın kabul koşulu DeepSeek ile beşinci duman koşusudur |
| K6 | Crossref'in rolü (plan §5'te yok; dilim 13'ten çıktı) | karar (22 Eylül 2026): aramadan çıkar, DOI/künye doğrulamada kalır; yedek arama kabul edilmedi (D87, dilim 13b) |
| K7 | Efor başına süre hedefi (plan §5'te yok) | karar (22 Eylül 2026): hızlı 5 / standart 10 / derin 15 dk **hedef**, zaman sınırı değil; toplama sınırı ve bekleme efora göre, sonra ölçülür (D88, dilim 13c) |
| K5 | Kampanyanın yeni sorusu (öneri: tıp ya da yaşam bilimi) | dilim 24'te sahip seçer |
